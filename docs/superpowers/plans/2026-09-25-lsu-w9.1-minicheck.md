# LSU W9.1 — 负载 W0 MiniCheck Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans（本单元为单文件定向负载，计划即全部内容）。

**Goal:** 实现 LSU 北极星负载 W0 MiniCheck（06-workloads.md r2 定义），作为全部 LSU 单元注入器的冒烟与传播调试负载。

**Architecture:** 单 C 文件定向负载（仓库 `workloads/directed/` 模式：源 + `gcc -O2 -static` 入库二进制 + 16-hex 确定 checksum），四个检测面：短数组校验和（FNV-1a）、数组内置换指针链、load/store round-trip（volatile 强制真读）、mmap+PROT_NONE guard page 链表（无故障运行永不触碰 page2）。

**Spec:** `docs/gem5-fi/lsu/06-workloads.md`（负载 W0 行原文）+ `docs/gem5-fi/lsu/09-implementation-plan.md` W9（滚动并行条款）。

## Global Constraints
- 二进制 `gcc -O2 -static`（CLAUDE.md：host 原生 aarch64，无交叉工具链）。
- 每次运行确定 golden output（06 原文）；native golden = `07568da9f3ad5665`（2026-09-25 实测 3 次全等）。
- 提交切分：本 commit = 源 + 二进制 + 本计划；**GOLDEN_IDS 注册 + gem5-SE golden 验证**在 W0 的 commit 落地后跟进（runner.py 当前被 W0 单元占用，staging 不混；guard page 的 mmap/mprotect 在 gem5-SE 的行为是首个 gem5 运行的验证点——若 SE 仿真不支持，回退为静态数组边界检测版并如实记录）。

## Task 1: MiniCheck 负载

- [x] **Step 1: 写 workloads/directed/mini_check.c**（四检测面，见源文件头注释）
- [x] **Step 2: 原生构建**：`cd workloads/directed && gcc -O2 -static -o mini_check mini_check.c`
- [x] **Step 3: 确定性验证**：连续 3 次运行输出全等 `07568da9f3ad5665`
- [x] **Step 4: 提交**：`git add workloads/directed/mini_check.c workloads/directed/mini_check docs/superpowers/plans/2026-09-25-lsu-w9.1-minicheck.md && git commit -m "feat(lsu): W9.1 workload W0 MiniCheck — 4-probe smoke workload (native golden 07568da9f3ad5665)"`
- [x] **Step 5（已完成 2026-09-25）**：GOLDEN_IDS 已注册；gem5 C4-LSU 无注入 golden == native `07568da9f3ad5665`（exit=0）；**发现**：gem5-SE 忽略 mprotect——guard page 在 SE 退化为 SDC 检测通道（Crash 通道仅 native/FS），已在源码头注与 runner 注释如实记录
