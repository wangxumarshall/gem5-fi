# v0.md 冲突框架重构修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 v0.md「与既有研究冲突」的框架从「不同发现」重构为「观测范式盲区」——伪冲突拆解（控制状态 vs 数据通路）、浮点伪标签自证段新增、ISA 假说降位为候选解释，并同步 v3.md §2.2/§7。

**Architecture:** 纯文档修改，两个文件（`docs/papers/xu/v0.md`、`docs/papers/xu/v3.md`）。论证链五步（全部证据已在仓库，零新实验）：(1) 拆解中枢为控制状态/数据通路，让「ROB/LSQ SDC=0%」与本文 DUE 主导结果一致化；(2) 用 DATE'25「未作研究」+Veritas 覆盖限制+PinDrop「根因未知」三重声明定位空白；(3) 用 Harpocrates LSQ 检出 0–20% 证明盲区是实测的工具下限；(4) 新增 core179 浮点伪标签证伪故事作现场自证；(5) SEVI wrong-offset 76% 作同构呼应。ISA 放大假说降位为与「实现差异」并列的候选解释。

**Tech Stack:** Markdown 文档编辑；grep 一致性自检；git commit/push。

**Spec:** 本对话中制定的修复方案（第 10 点修复方案，含 §2 逐处修改清单、§3 替换文本草案、§4 文献增补、§5 诚实边界纪律）。证据源：`docs/papers/xu/hot-case.md`（§3 六层排除法、§5 结论句）、`docs/papers/xu/hot-case-other-cases.md:22`（Harpocrates 0–20% + 范式论证原句）、`docs/cases/core179-microarch-rootcause-synthesis/paper2026_zh.md` §1.1/§2.2（Harpocrates/SEVI/PinDrop/Veritas 转述）、`mspc_paper_zh.md:137`（「仪器的缺席」句）。

## Global Constraints

- **诚实纪律（CLAUDE.md 强制）**：所有「很可能是盲区」类表述保持再解释假说语气，禁止写成「文献错了」；Harpocrates 检出率只用于锚定工具能力，**不得**用于攻击 TC'23 的注入转化率结论（四个「0%/空白」性质区分表见 Task 1 备注）。
- **One-patch-per-unit**：本计划全部修改合为**一个 commit**（单一连贯修改单元：冲突框架重构），不拆分。
- **分支**：当前在 `fi-wangxu`（非 main），直接提交并 push 到 `fi-wangxu`。
- **Commit message**：`docs(papers/xu): v0 冲突框架重构——伪冲突拆解+范式盲区论证+ISA假说降位`，**不得**以 `Co-Authored-By: Claude <noreply@anthropic.com>` 结尾。
- **不改动** `hot-case.md` / `hot-case-other-cases.md`（它们是证据源，只读）。
- 本计划只修第 10 点 + 顺带 L4/L9（与第 11 点重叠的两处表述）+ 顺带 18→19 笔误；**不处理**其余 P0 问题（三案例单机口径、D2/D3 降级等），它们是独立 patch。
- SEVI 76% wrong-offset 必须与 FMA 92% 上下文同报，防止反向过度声称。

---

### Task 1: v0.md 冲突框架重构（主体修改）

**Files:**
- Modify: `docs/papers/xu/v0.md:4-9`（L4 定调句、L5-7 三案例段之后新增一段、L8 对立句式与 ISA 段降位、L9 结尾收窄与 18→19）

**Interfaces:**
- Consumes: hot-case.md §3（六层排除法事实：固定 seed 单跑 9000+ 次全过；密集 GEMM/SVD/纯 FMA/整数/CRC 全零失败；损坏固定 x[0] 多位 21-32 bit 混叠；Eigen 是当时唯一在跑的浮点密集负载）、hot-case.md §5 结论句（「浮点负载上发生的 SDC 不等于浮点单元的 SDC……指令只是激励的载体」）、hot-case-other-cases.md:22（Harpocrates LSQ 0–20%、范式偏向论证）、paper2026_zh.md §2.2（SEVI 76%/24%、FMA 92%；PinDrop「未知且超出本文范围」；Veritas「只覆盖算术 FU」）、DATE 2025 既有条目（v0 L35「未作研究」）。
- Produces: v0.md 新论证链正文（供 Task 2 的 v3.md §2.2/§7 同步引用同一框架）。

**四个「0%/空白」性质区分纪律（编辑时对照，防止混用被审稿人反杀）：**

| 声明 | 性质 | 本文用法 |
|---|---|---|
| TC'23：ROB/LQ/SQ 注入 SDC=0% | 注入转化率（控制状态注入点） | 只说「一致，本文复证」 |
| DATE'25：buffers/queues「未作研究」 | 明示空白 | 本文填充对象 |
| Veritas/PinDrop 覆盖限制 | 范围声明 | 空白旁证 |
| Harpocrates：LSQ 检出 0–20% | 工具能力实测 | 盲区最强独立证据，锚定「仪器的缺席」 |

- [ ] **Step 1: 修改 L4 定调句**

将：
```
得到了一些与既有研究结论不同的发现。
```
改为：
```
得到了一些落在既有研究的观测范式难以显影的区域的发现。
```

- [ ] **Step 2: 在 L7（第三类通路异常段）之后、L8（三类共同特征段）之前，插入浮点伪标签自证段**

插入文本（独立成段）：
```
这个盲区不是假设——我们自己也踩过。上述装载通路案例最初的标签正是"浮点 SDC"：循环测试中仅有的两次失败全部落在 Eigen 稀疏 Cholesky 与 float GEMM 上，表象强烈指向浮点单元损坏。六层假设排除法证伪了这个标签：固定失败 seed 在故障核单跑九千余次全部通过（排除软件 bug）；测试矩阵中密集 GEMM、SVD、纯 FMA、整数与 CRC 测试全部零失败（排除浮点流水线本身）；损坏固定发生在求解向量的单个元素、呈多位混叠而非单比特翻转（排除数据边界问题）。Eigen 报错，只因它是当时唯一在跑的、既浮点密集又涉及复杂数据搬运的负载。浮点负载上发生的 SDC 不等于浮点单元的 SDC：指令只是激励的载体，任何经过公共数据搬运网络的功能单元都可能成为"背锅者"。指令级功能测试天然偏向"输出即结果"的算术单元；数据搬运通路的故障，需要另一种观测面——同一访存的内存真值与寄存器实收值的对照——才能显影。这恰是既有舰队测试范式没有提供、而本研究的取证方法所补上的观测面。
```

- [ ] **Step 3: 重写 L8 开头的对立句式（伪冲突拆解）**

将 L8 开头：
```
这三类发现有一个共同的特征：故障均指向处理器中枢与内存子系统里的缓冲、队列、路由结构，而非现有 SDC 研究长期聚焦的后端执行单元（如整数逻辑单元、浮点与向量加乘器）。
```
改为：
```
这三类发现有一个共同的特征：故障均指向处理器中枢与内存子系统中流动的数据通路——转发、回填合并、字节选路。这初看与既有研究矛盾：主流结论认为 SDC 集中于后端执行单元，而 ROB、访存队列的 SDC 转化率为零。但把"中枢结构"拆开看，这个矛盾大部分会消失。既有研究测得的"零"，落在这些结构的控制状态上——队列指针、依赖标记、映射表项。这类错误在指令提交前就会被依赖检查拦截、转化为崩溃；我们的控制结构注入实验复现了同一规律（DUE 主导、SDC 接近零），在这一层上双方一致。真正出事的部位是这些结构中流动的数据。对这一区域，既有研究并非测得"安全"，而是从未测量：DATE 2025 明言此类结构"未作研究"，Veritas 的注入只覆盖算术功能单元，PinDrop 的访存测试族则声明其根因"未知且超出本文范围"。检测工具在这一区域的能力有实测下限：Harpocrates 对功能单元接近满分检出，对 LSQ 仅 0–20%，是所有结构中最低的。SEVI 自己的舰队数据也含有通路签名：访存类 SDC 中 76% 表现为错误偏移读取——与本文的"撕裂移位"同构，尽管其 SDC 事件总量的 92% 仍来自 FMA 指令。换言之，"中枢 0%"很可能测的是仪器的缺席，而非缺陷的缺席；既有文献的单元归因停在算术单元，缺的是把数据通路故障显影的观测面——本文补的正是这个。
```

- [ ] **Step 4: L8 ISA 段降位**

将 L8 中 ISA 论证段的引入句：
```
由此我们提出一个怀疑：这类结构的风险，是否在 ARM 架构下存在额外的、结构性的放大因素。
```
改为：
```
在这一方法论归因的基础上，ISA 层面还存在一个候选的结构性放大因素，可能使这块盲区在 ARM 平台上承载更高的负载。
```

同时在 L8 段尾（「……叠加的结构性放大效应。」之后）追加：
```
需要诚实说明：这一放大效应是候选解释之一；全部现场证据来自单一厂商的单一微架构实现，实现差异（各 SoC 厂商的时序签核与防护带差异）是并列的竞争解释，二者的裁决需要同代工艺、同等配置的跨 ISA 对照实验。
```

并在 ISA 两条论证（Load-Store 密度、31 寄存器）之后、上句之前，插入桥接句（缓解论证链错位：论证的是重命名高压、出事的是数据通路）：
```
更高的发射与访存密度还意味着转发窗口占用率的上升——数据交付通路上的相位竞争概率随之放大；现场复现中插入一条 no-op 指令即可将触发率从 100% 降至 10%-20%，正是这种相位竞争的直接表现。
```

- [ ] **Step 5: L9 结尾收窄 + 18→19**

将 L9：
```
基于这一怀疑，我们以鲲鹏 920 作为当前主流 ARM 服务器核心的典型代表，对其处理器中可识别的 18 个微架构结构进行系统、全面的故障注入实验，逐一衡量每个微架构结构在 ARM 架构下的 SDC 脆弱性，以此验证并回答中枢与内存子系统是否确如推测的那样，构成了 ARM 架构下 SDC 风险被现有研究低估的关键区域。
```
改为：
```
基于这一假说，我们以鲲鹏 920 作为大规模部署的 ARM 服务器代表平台，对其处理器中可识别的 19 个微架构结构进行系统、全面的故障注入实验，逐一衡量每个微架构结构在 ARM 架构下的 SDC 脆弱性。注入实验能够回答的是两件事：ARM 平台上各结构的 SDC 暴露面排序，以及注入故障签名与现场取证的匹配程度。
```

- [ ] **Step 6: 文献笔记区（L18-43）增补**

1. L22（TC'23 相关行）「"ROB、L/S Queue上SDC为 0%"」后追加注：
   `（注入点为控制状态；论文自身给出机理解释：提交前依赖检查必崩，控制流结构免疫 SDC）`
2. L23-24（Harpocrates 条目）追加：
   `实测：对功能单元接近满分检出，对 LSQ 的 SDC 检测率仅 0–20%（所有结构最低）——工具能力盲区的最强独立证据`
3. L38-39（SEVI 条目）追加：
   `访存类 SDC 两模式：错误偏移读取 76% / 数据损坏 24%（前者与本文"撕裂移位"同构）；FMA 占 SDC 事件 92%`
4. L37-38（PinDrop 条目）追加：
   `Data-Move 测试族论文明言"底层硬件根因未知且超出本文范围"`
5. L32（Veritas 条目）追加：
   `注入只覆盖算术 FU，AGU 仅有限建模`

- [ ] **Step 7: 验证 v0.md 修改**

Run: `grep -n "仪器的缺席\|背锅者\|候选解释之一\|19 个微架构" docs/papers/xu/v0.md && grep -c "18 个微架构" docs/papers/xu/v0.md; echo "exit=$?"`
Expected: 前一 grep 命中新文本各处；后一 grep 计数为 0（grep -c 返回 0 时 exit code 为 1，`echo exit=1` 正常）。
再通读修改后的 L4-L9 全文一遍，确认：浮点伪标签段位于三类通路异常之后、ISA 论证之前；ISA 段以「在这一方法论归因的基础上」引入；无「与既有研究结论不同」残留（Run: `grep -n "与既有研究结论不同" docs/papers/xu/v0.md`，Expected: 无输出）。

- [ ] **Step 8: Commit（暂不 push，与 Task 2 合为一个 commit）

见 Task 2 Step 4。本任务不单独提交。

---

### Task 2: v3.md §2.2/§7 同步 + 合并提交

**Files:**
- Modify: `docs/papers/xu/v3.md:39-47`（§2.1 末段与 §2.2 冲突表、冲突解释段）、`docs/papers/xu/v3.md:184`（§7「与既有研究的再定位」段）

**Interfaces:**
- Consumes: Task 1 建立的框架（控制状态/数据通路拆解、Harpocrates 0–20%、浮点伪标签自证、ISA 降位）。
- Produces: v0/v3 两版口径一致的冲突叙事。

- [ ] **Step 1: 修改 §2.1 末段（v3.md:47）**

将：
```
这些工作有两个共同点值得注意：其一，实验平台与 fleet 数据均以 x86 为主体；其二，缓冲、队列、路由类结构要么被报告为 SDC 为零（ROB/LSQ），要么被明确标注"可能破坏流水线状态但 SDC 概率低，未作研究"（DATE 2025 的原文表述）。ARM 平台上的中枢结构暴露面，处于这批工作的视野之外。
```
改为：
```
这些工作有两个共同点值得注意：其一，实验平台与 fleet 数据均以 x86 为主体；其二，缓冲、队列、路由类结构的控制状态被报告为 SDC 为零（ROB/LSQ，机理自证：提交前依赖检查必崩），而其中流动的数据通路要么被明确标注"未作研究"（DATE 2025），要么受限于注入覆盖（Veritas 只注入算术 FU）与工具能力（Harpocrates 实测对 LSQ 的 SDC 检测率仅 0–20%，为所有结构最低）。中枢数据通路是既有研究的观测盲区，而非被证明安全的区域；ARM 平台上的这块暴露面，处于这批工作的视野之外。
```

- [ ] **Step 2: 修改 §2.2 表 1 与其后的冲突解释段（v3.md:53-60）**

表 1 中该行：
```
| ROB/LSQ 等中枢 | SDC 0%（报告值） | 未直接观测到 SDC，但转发/缓冲数据通路为致命位点 |
```
改为两行：
```
| 中枢控制状态（ROB/LSQ 指针、依赖标记） | SDC 0%（注入转化率，机理自证） | 一致：本文控制结构注入 DUE 主导、SDC≈0，复证该结论 |
| 中枢数据通路（转发、回填合并、字节选路） | 未测量（DATE'25 明言未作研究；Harpocrates LSQ 检出 0–20%） | 致命位点：回填 90.9%、转发相位错开 100% |
```

将其后解释段：
```
对这一冲突，我们给出的解释不是"既有研究错了"，而是平台差异：x86 研究测得的是这些结构在 x86 ISA 负载下的暴露面，而 ARM 的 ISA 设计可能在中枢结构上叠加了结构性的放大效应。这一假说的论证与验证是本文的主体。
```
改为：
```
因此双方在控制状态层并不冲突——本文的注入结果复证了"控制结构免疫 SDC"。真正的冲突只存在于数据通路层：既有研究从未测量这块区域（明示的空白、覆盖限制与工具下限三重声明），而本文的现场取证显示它恰是致命位点。指令级功能测试天然偏向"输出即结果"的算术单元；本文现场案例最初的"浮点 SDC"标签正是这一范式偏差的产物，六层假设排除法证伪了它（第 3.2 节）。在这块观测盲区之上，ARM 的 ISA 设计可能进一步叠加结构性的放大效应（第 4 节），与单一厂商实现差异构成并列的候选解释，其裁决需要跨 ISA 对照实验。
```

- [ ] **Step 3: 修改 §7「与既有研究的再定位」段（v3.md:184）**

将该段整体替换为：
```
**与既有研究的再定位。** 本文结果不推翻 x86 研究的结论，而是拆解它：控制状态层的"ROB/LSQ SDC 为零"有机理自证，本文的注入结果复证了它；数据通路层则从未被测量——DATE 2025 明言未作研究，Veritas 的注入只覆盖算术 FU，Harpocrates 实测现有工具对 LSQ 的 SDC 检测率仅 0–20%（所有结构最低），PinDrop 的访存测试族声明其根因未知。"中枢 0%"测的很可能是仪器的缺席，而非缺陷的缺席；本文现场案例最初的"浮点 SDC"标签及其证伪，正是这一范式偏差的现场实证。在此盲区之上，现场与仿真证据共同表明，ARM 平台上同样的结构承载着更高密度的重命名调度与更大规模的路由网络，其数据通路的暴露面被放大到首位——这一 ISA 归因是与实现差异并列的候选解释。两者共同指向一个更一般的命题：SDC 的微架构分布是观测范式、ISA 设计与结构实现的函数，不存在跨架构通用的"安全结构清单"。
```

- [ ] **Step 4: 验证两文件一致性并 Commit + Push**

验证 v3.md：
Run: `grep -n "仪器的缺席\|中枢控制状态\|候选解释" docs/papers/xu/v3.md && grep -n "SDC 0%（报告值）" docs/papers/xu/v3.md; echo done`
Expected: 第一 grep 命中新文本；第二 grep 无输出（旧表行已替换）。

通读两文件修改区域，核对全局约束：无「文献错了」式断言；Harpocrates 数据未与 TC'23 注入结论混用（区分表四行性质未被合并表述）；SEVI 76% 与 FMA 92% 同报。

Commit：
```bash
git add docs/papers/xu/v0.md docs/papers/xu/v3.md
git commit -m "docs(papers/xu): v0 冲突框架重构——伪冲突拆解+范式盲区论证+ISA假说降位

- v0: 「不同发现」→「观测范式盲区」；中枢拆解为控制状态（与x86一致，本文复证）与数据通路（未测量区域）
- v0: 新增浮点伪标签证伪自证段（hot-case.md §3 六层排除法）
- v0: Harpocrates LSQ 检出 0-20%/DATE'25 未作研究/Veritas+PinDrop 覆盖限制定位空白；SEVI wrong-offset 76% 同构呼应（与FMA 92%同报）
- v0: ISA 放大假说降位为与实现差异并列的候选解释；补发射密度→转发窗口→相位竞争桥接句；结尾验证承诺收窄；18→19
- v3: §2.1/§2.2/§7 按同一框架同步；表1 中枢行拆为控制状态/数据通路两行"
git push origin fi-wangxu
```
Expected: commit 成功、push 成功；`git log -1 --stat` 显示仅两个文件变更。

---

## 自检记录（Self-Review）

1. **Spec 覆盖**：修复方案 §2 的七处修改清单 → Task 1 Step 1-6 逐条对应（L4→S1、新增段→S2、L8 对立句→S3、ISA 降位→S4、L9→S5、文献区→S6）；§3 草案 A/B/C → S3/S2/S4；§5 诚实边界 → Global Constraints + Task 2 Step 4 核对项；§7 同步修改 → Task 2；§6 数字核验清单属于投稿前工作，不在本计划范围（已注明为二手转述）。
2. **占位符扫描**：无 TBD/TODO；所有替换文本均为完整成文。
3. **一致性**：v0 与 v3 使用同一术语（中枢控制状态/中枢数据通路、仪器的缺席、候选解释）；「19 个微架构」与 v1/v2/v3 既有口径一致。
