# OoO W7 FP/SIMD 注入器 Implementation Plan

> **Spec**：04-design-matrix.md D56-D91 + 06 §4 W7（D62-66/D72/73/76 已按北极星合并条款并入向量行，a8697ca9）。机制事实全在 findings.md「W7 FP/SIMD 设计 spike 结论」（行号已核）。先例：W4（RAT/Freelist 11+7 模式）、W5（ROB 9 模式）、W6（Decode 8 模式）。
> **依赖硬约束**：W7.1 排 W6 T2 后；W7.2/3 排 W5 T3-4 后；W7.4 排 W5 T5-7 后。

### Task 1: W7.1 — FP Decode（D56-D61）
- [x] W6 引擎 + `fpOnly` 过滤（先明确口径：CHAOSFPU.cc:88-98 isFpOpClass 含标量+SimdFloat 但不含整数 SIMD——按 04"FP/SIMD"口径取 SimdFloat*∪Float*，文档化）；FP opcode 位集 bits23:10（D56/D57）；**FP swap 规则表新增**（FADD↔FSUB/FMUL↔FDIV 等操作数兼容对，主机 gcc 真编码核验——W6 先例；D58）；寄存器号 D59/D60 零改动（kRegBits 已覆盖 Vd/Vn/Vm，effective() 谓词保留筛 fp.isa 交错路径）；D61=引擎+新谓词 `new_opClass != orig_opClass`（诚实近似：无独立路由位，opClass→FUPool capability；观测=FU/latency 效应）。〔执行注 2026-09-24，实现代理，未 commit〕6 模式全落地：`fp_opcode_bitflip`/`fp_opcode_bitflip2`（kFpOpcodeBits=enc[23:10]）/`fp_opcode_swap`（14 规则 kFpSwapRules：标量 fadd/fsub、fmul/fdiv、fmax/fmin、fmaxnm/fminnm、fmadd/fmsub、fnmadd/fnmsub、fcmp/fcmpe + SIMD fadd/fsub、fmax/fmin、fmaxnm/fminnm、fmla/fmls、fcmeq/fcmge、fmul/fdiv、fabs/fneg）/`fp_reg_bitflip`/`fp_reg_bitflip2`（W6 kRegBits 引擎零改动+fpOnly；验证中发现并修复 two_bits 检查漏 FpRegBitflip2 的单比特 bug）/`fp_route_bit`（kFpRouteBits=enc[28:24] 有效位扫描+opClass!=orig 谓词+No_OpClass/"unknown" 排除+诚实跳过日志+OpClassStrings 新旧名）。**互换表闭环核验**（/tmp/w71/verify_rules.py，主机 gcc+objdump .inst 往返）：320/320 成员翻转 PASS（伙伴 mnemonic+寄存器操作数全等，S/D/H×.2s/.4s/.2d×4 组寄存器）+ 46/46 负例零误匹配（fmov/fsqrt/fmulx/fabd/fcmgt/facge/frint/fccmp/fcsel/scvtf/by-element/FP16-3-same/fcvtl/fcvtn）；掩码逐位规格程序化生成（fadd/fsub 排 FABD 需 pin bit29=0、fcmeq/fcmge 排 FCMGT 需 pin bit23=0、fmul/fdiv 排 FMULX 需 pin bit29=1、fabs/fneg 排 FRINT 需 pin Rm=0——手工掩码两次算错后改脚本派生）。**验证**：构建零警告（首次构建撞并行 ROB/IQ 代理 scons 竞态 Error 127，等待后重建通过）；golden 45737cc9a76c0dce 精确回归×2；6 模式 nbody（FP 负载）注入日志全到位（D56 bit[22] fmov→unknown / D57 bits[22,10] / D58 fsub→fadd swap_rule=s_fadd_fsub / D59 bit[0] fmov→fmov regs_moved=1 / D60 bits[3,9] / D61 bit[27] FloatMisc→IntAlu fmov→bl）+ 编码 diff 6/6 可复算 + 同 seed 复现 6/6 字节一致；D58 种子扫描 101-106 六抽六不同（s_fmul_fdiv 双向×4+s_fadd_fsub 双向×2，全 Masked——单错命中错误路径比例高，campaign 级统计归 W8）；runner 端到端路由（C3 manifest sub_field=fp_opcode_swap → `[runner] RESULT: classification=Masked faults_injected=1`+outdir 日志 mode=fp_opcode_swap）；W6 整数模式引擎回归（opcode_bitflip Masked；reg_bitflip 该 seed 合法 Crash=vaddr 0 页表故障，路径未被 W7 改动触碰）。**诚实边界**：fpOnly=Float*∪SimdFloat*（整数 SIMD 出 W7.1 范围，三处文档化）；D61 int→FP 方向不可达（fpOnly 闸门）；kFpOpcodeBits[23:10] 与 Rm[20:16] 重叠（沿用 W6 kImmBits 口径，日志带位号）；SIMD 向量互换规则未在向量负载上触发演示（nbody 纯标量 FP；规则本身已 objdump 双向核验）——libjpeg 补跑留 W8。进度与全程记录 /tmp/ooo_w71_progress.txt。

### Task 2: W7.2 — VecRegClass RAT 族（D62-D71 合并行 + D66/D71 stale_read）
- [x] CHAOSRenameMap 加 `targetClass` 参数（Int/Vec 两值即可，Float 惰性）：放宽 5 处 int 闸门（:194/:429/:471/:605/:706）、XZR 守卫按类条件化、pickAllocatedPhysReg + collectRobActiveDests 加类参数与 vecPhysRegId（regfile.hh:177）；**归因设计决策**：RAT 钩子无 opClass——D62-66 与 D67-71 同路径执行，归因从 commit trace 后置（文档化）。全部 11 模式 × Vec 类参数化（map_bitflip/2/swap_to_active/f5_rat_stuck/stale_read/hb_bitflip/2 优先）。〔执行注 2026-09-25，实现代理，未 commit〕`targetClass` int|vec（panic 拒未知值；无 "float"——W1.2 平台发现 AArch64 标量 FP 走 VecRegClass，D62-66 按 04 D62 合并条款并入 vec，决策文档化在 .hh/.py）；5 闸门→target_class、XZR 守卫→archIdxValid（vec=V0-V31，32-43 Special/Interleave 排除=banked 类比，向量无零寄存器）、rng%31→numArchTargetRegs(31/32)、numIntPhysRegs/intPhysRegId→类助手、pickAllocatedPhysReg+VecRegClass 分支、collectRobActiveDests 类过滤（vec 活跃池证据）；oldphys 族保持 int-only（vec+oldphys 构造时 warn+惰性）。vec 日志 arch=V<n>+", class=vec"（int 行字节不变）。**验证**（libjpeg NEON 负载，seed 20260925，单射）：D67 map_bitflip SDC（FINAL=274b07a1f6d5a18f；arch_reg=vec[0] 13→9 bit2 可复算）；D68 map_bitflip2 Crash（panic "Dependency graph 8 (vector)"——vec 域 panic 证据；13→8 bits(2,0) popcount=2）；D69 swap_to_active Crash（panic "Dependency graph 15 (vector)"；new_phys=15(active)∈rob_active_dests=[15@6 12@8] 全 <48 vec 池）；D70 f5_rat_stuck Hang 类（21min 预算杀，~24× 正常时长；ARMED arch=V28 bit=4 polarity=1 num_phys=48 + exposure 0→16 + 越界诚实跳过 47→63）；D66/D71 stale_read Masked（arch=V0 attempted=14/kept=15）；hb_bitflip Hang 类（newPhysReg 14→12 位 1，doSquash HISTORY_CONSUMED 完整休眠链，泄漏饿死初始空闲 4 的 vec 池）；hb_bitflip2 Crash（14→13 bits(1,0)，panic "Dependency graph 13 (vector)"）。4/4 位模式数学复算 PASS；同 seed a/b 复现 10/10 日志字节一致；golden 45737cc9a76c0dce 无注入回归 PASS；int 回归 map_bitflip smoke 字节 W4 格式（arch_reg=int[0]，零 class=vec）FINAL=golden；runner E2E（sub_field=swap_to_active_vec）`classification=Masked faults_injected=1`+outdir 日志 class=vec。

### Task 3: W7.3 — VecRegClass 空闲表（D72-D77 合并行）
- [x] CHAOSFreeList mark_free/mark_free_event 类参数化：三处 int 硬编码（:142 类闸门/:255 numFreeRegs/:159 vecPhysRegId）+ **按类阈值**（vec 池初始空闲 4——int 的 ≤8 对 vec=全池；北极星 D75 的 ≤6 需按「初始空闲 4」重导为 ≤2 或 free==0，findings D75 校准结论）；finalSummary 扩 vec 行。〔执行注 2026-09-25，实现代理，未 commit〕`targetClass`+`eventThresholdVec`（默认 0=pool drained，取 findings W1.5b「free==0 隔离真实压力窗口」推荐；04 的 ≤6 对 44 arch vec 映射/48 phys=初始空闲 4 的平台为恒真——退化为 D74 固定间隔，偏差依据文档化在 .hh/.py/ooo_proxy）；全部 7 模式类参数化（闸门/dup_watch/numPhys/physRegId/isFree/numFreeRegs/阈值），finalSummary +final_free_vec+target_class。**验证**（libjpeg，seed 20260925，单射）：D74 mark_free SDC（FINAL=1e71edf938510d4d；readded_allocated_idx=33 + DUPLICATE_ALLOCATION: idx 33 二次分配证据；exit final_free_vec=4）；D75 mark_free_event Masked（**阈值重导实证**：num_free_vec_after_pop=0 (threshold=0) 精确命中"pop 耗尽最后空闲 vec 项"窗口 + readded=39 + DUPLICATE_ALLOCATION: idx 39；exit final_free_vec=5）；D77 drop_release Masked（suppressed_release_idx=7, num_free_vec_at_drop=3 "queue stays at 3, permanently -1"；exit final_free_vec=3——-1 收缩在退出行可见）；同 seed a/b 3/3 字节一致；int 回归 mark_free smoke 注入 W4 格式（class=int）→ 合法 D17 Crash（panic "Dependency graph 1 (integer)"）；runner E2E（sub_field=mark_free_event_vec）`classification=Masked faults_injected=1`。**顺带修复 runner 预存缺陷**：DUPLICATE_ALLOCATION 证据行被通用行计数器误计为第二注入（D17/D18/D74/D75 全部 faults=2+G5 VIOLATION；与 CHAOSCache "protection:" 前例同类）——加 skip 后 E2E faults_injected=1 干净。

### Task 4: W7.4 — FP Dispatch/ROB（D83-D91）
- [x] **D83-D85 零新代码**（W5 现有模式 × FP 负载 campaign——属 W8 执行）；D86 零新代码（CHAOSIQ 直接可用，可加 opClass 过滤增强）；D87-D91 = W5 tag 族（D50-54 机制）+ 类参数化（collectIntDestSlots/collectRobActiveDests/4 处闸门）。**排 W5 T5-7 落地后**。〔执行注 2026-09-25，实现代理，未 commit〕**实现**：CHAOSIQ +`targetClass`(int|vec，panic 拒未知值) +`fpOnly`（§2.5 三模式 opClass 过滤 Float*∪SimdFloat*，触发行带 op_class 名）；tag 族（swap/bitflip/bitflip2/stuck/stale_read）+ready_early 全类参数化——collectIntSrcSlots→collectSrcSlots（类过滤）、tag 域按类 [0,numVecPhys)（C3 vec=48）、physRegId 按类、int 保留 W5 isFloating/isVector 闸门/vec 按 VecRegClass 槽位定种群（标量 FP+FP SIMD+整数 SIMD 全走 VecRegClass 重命名，W1 平台发现，libjpeg 整数 NEON 亦入种群）；CHAOSROB destid 族（bitflip/bitflip2/swap_active/stuck+readback）同模式参数化（4 处 int 闸门+活跃池类过滤），PC/done/ptr 族保持类无关（统一 ROB）。ooo_proxy `--iq_target_class/--iq_fp_only/--rob_target_class`；runner iq 块 "_vec"/"_fp" 后缀、rob 块 "_vec" 后缀（与 W7.2/3 rat/freelist 块同约定）。**发现并修复 W5 D54 潜伏缺陷**：stale_read 原实现别名 departed µop 的 _srcIdx 指针，而 DynInst 数组存储经 inst-pool 回收（nbody 实测扫到 index 60128 的"vec tag"、sn=0，注入悬垂指针→getReg panic）——修复=候选必须在合法域 [0,num_phys)（域外诚实跳过）+注入 id 从 regfile 永久表重建（targetPhysRegId）；int 版同缺陷一并受益。**验证**：构建零警告×2（含修复后重建）；golden 45737cc9a76c0dce×3 PASS；D87-91×{nbody,gemm,libjpeg} 15/15 注入（日志 class=vec+tag_domain=[0,48)，old/new/bits 可复算）全 Masked（seed 20260925 定向单射）；D86 ready_early vec×3 注入 + fpOnly src_ready_bitflip×3（op_class=FloatAdd 证据行）+ wake_phase fpOnly + wake_omit fpOnly→Timeout（丢 FP wake 致依赖永久未就绪，F6 设计结局）+ 无过滤 plain 版注入；vec destid 族×3：swap_active/stuck 全 Masked（活跃池含 vec id 证据）、bitflip(bitflip2) gemm/jpeg Timeout（错 dest id→真依赖永不唤醒→ROB 塞满，TC'23 依赖拦截设计结局）/nbody Masked；D83 pc_bitflip、D84 done_early、D85 done_early_event（ROB 103/128>80% 门开启）FP 负载注入日志全到位（零新代码证实）；int 回归 tag_bitflip→确定性 Crash（野地址 SEGV，复跑一致）+destid_swap_active Masked；同 seed 复现 6/6（vts/vdsa/srbfp/vstale_v2/r_tagbf/vdb2 楔死 全字节一致）；runner E2E×2（iq sub_field=tag_swap_vec、rob sub_field=destid_swap_active_vec）`classification=Masked faults_injected=1`+outdir 日志 class=vec。诚实边界：fpOnly 过滤在触发侧（src_ready_bitflip 的受害者侧仍类无关，已注释）；D90 nbody/gemm 种子下 arm 到已处极性的位（值掩蔽无观测变化，jpeg 亦然——不同 seed 扫描归 W8）；ctest L2 补跑（2026-09-25，代理B+编排者复核，产物 /tmp/w74_l2/）：D87 tag_swap_vec@nbody 两遍法 Masked + `no_divergence`（4083949 对齐对、tick_tol=0、max_tick_drift=0、双 trace 字节等长 48497117 自洽，注入行 sn=141 old=44→new=1 与定向单射逐字段一致）；W5 int tag_bitflip 对照（seed 20260925，sn=157 old=82→86 bits=(2)）Crash + L2 ②指令流改变（presence 级）——**诚实发现：该极早 Crash 的 trace 为 0 字节**（panic 早于首次周期 gzflush(4096 行)，整缓冲丢失；属 W2.1 文档化「损失<4096 行」包络的边界端点——极早 Crash 样本 L2 退化为 presence 级，五分类需 run 侧至少一次 flush）；截断抢救路径经真实参照 trace 流中截断（head -c 20000000）合成检验工作正常（`truncated_no_divergence_in_prefix`、前缀 1689204 对齐、无 gzip trailer 分支正确触发）。进度 /tmp/ooo_w74_progress.txt。

**通用**：每 Task 三件套验证 + 注入日志（值可复算/活跃池含 vec id）+ 同 seed 复现 + golden 回归 + ctrace L2 一张 + runner 路由；代理不 commit；FP 负载用 polybench/embench-nbody/minver/libjpeg。
