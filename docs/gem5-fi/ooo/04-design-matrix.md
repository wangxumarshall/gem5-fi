# 04 · 位置×模型设计矩阵（91 个设计单元）

> 来源：《gem5-fi-OoO单元故障注入方案V1.0.xlsx》工作表「1.位置x模型矩阵」R2–R92，**逐行忠实提取，未做任何改写**（生成器：`extract.py`，可重跑复现）。
> 行 ID `D01–D91` 为本目录新增的稳定引用编号，按 Excel 行序分配；`Excel行` 列为原表行号，供回溯。后续文档/提交/注入器实现统一用 D 编号指代设计单元。
> 列语义：13 列与源表一致；五条贯穿性边界条件见 `00-overview.md`，频率档位定义见 `02-frequency-and-sampling.md`。

## 索引（扫描用紧凑表）

| ID | Excel行 | 单元 | 位置标签 | 故障类型 | 适用频率 | 事件触发条件 | 适用负载 | 计数基准 |
|---|---|---|---|---|---|---|---|---|
| D01 | R2 | Int Decode | opcode字段·单比特翻转 | 单比特翻转 | F0 / F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D02 | R3 | Int Decode | opcode字段·双比特翻转 | 双比特翻转 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D03 | R4 | Int Decode | opcode字段·换值 | 换值（结构化） | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D04 | R5 | Int Decode | 源/目的寄存器号字段·单比特翻转 | 单比特翻转 | F0 / F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D05 | R6 | Int Decode | 源/目的寄存器号字段·双比特翻转 | 双比特翻转 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D06 | R7 | Int Decode | 立即数字段·单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | Embench | 运行计数 |
| D07 | R8 | Int Decode | 立即数字段·双比特翻转 | 双比特翻转 | F0 / F1 | —（不涉及事件触发） | Embench | 运行计数 |
| D08 | R9 | Int Decode | 立即数字段·符号扩展位定向翻转 | 状态（结构化） | F0 / F1 | —（不涉及事件触发） | Embench、GAP | 运行计数 |
| D09 | R10 | Int Decode | 立即数字段·子字段拼接错位 | 错位拼接（结构化）：子字段移位错位 | F0 / F1 | —（不涉及事件触发） | Embench、GAP | 运行计数 |
| D10 | R11 | Int Decode | 裂解控制位·状态翻转 | 状态（结构化） | F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D11 | R12 | Int Rename | RAT映射字段·单比特翻转 | 单比特翻转 | F0 / F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D12 | R13 | Int Rename | RAT映射字段·双比特翻转 | 双比特翻转 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D13 | R14 | Int Rename | RAT映射字段·换值(固定间隔) | 换值（结构化） | F0 / F1 | —（不涉及事件触发） | CoreMark、分支误预测密集核 | 运行计数 |
| D14 | R15 | Int Rename | RAT映射字段·换值(误预测事件触发) | 换值（结构化，事件触发版） | —（不适用固定间隔） | 误预测恢复瞬间 | 分支误预测密集核 | 事件覆盖计数 |
| D15 | R16 | Int Rename | RAT映射字段·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D16 | R17 | Int Rename | RAT映射字段·读到旧数据 | 错位拼接（结构化）：读到旧数据 | —（不适用固定间隔） | 该 RAT 表项被下一次重命名写入覆盖的瞬间 | CoreMark、Embench | 事件覆盖计数 |
| D17 | R18 | Int Rename | 空闲表·重复分配(固定间隔) | 状态（结构化）：重复分配 | F1 / F2 | —（不涉及事件触发） | CoreMark、长依赖链压力核 | 运行计数 |
| D18 | R19 | Int Rename | 空闲表·重复分配(事件触发) | 状态（结构化）：重复分配 | —（不适用固定间隔） | 空闲表剩余项数低于阈值（建议剩余 ≤8 项时） | 长依赖链压力核 | 事件覆盖计数 |
| D19 | R20 | Int Rename | 空闲表·丢失释放 | 时序（结构化）：丢失释放 | F1（持续到运行结束，属于一次性触发后持续影响） | —（不涉及事件触发） | Embench、长依赖链压力核 | 运行计数 |
| D20 | R21 | Int Rename | 空闲表头/尾指针·单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | CoreMark | 运行计数 |
| D21 | R22 | Int Rename | 空闲表头/尾指针·双比特翻转 | 双比特翻转 | F0 | —（不涉及事件触发） | CoreMark | 运行计数 |
| D22 | R23 | Int Rename | 空闲表头/尾指针·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | CoreMark、长依赖链压力核 | 运行计数 |
| D23 | R24 | Int Rename | 重命名检查点·单比特翻转 | 单比特翻转 | —（不适用固定间隔） | 检查点建立后到被消费（误预测恢复或释放）之间的窗口内 | 分支误预测密集核 | 事件覆盖计数 |
| D24 | R25 | Int Rename | 重命名检查点·双比特翻转 | 双比特翻转 | —（不适用固定间隔） | 检查点建立后到被消费之间的窗口内 | 分支误预测密集核 | 事件覆盖计数 |
| D25 | R26 | Int Dispatch/ROB | ROB的PC字段·单比特翻转 | 单比特翻转 | F0 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D26 | R27 | Int Dispatch/ROB | ROB的PC字段·双比特翻转 | 双比特翻转 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D27 | R28 | Int Dispatch/ROB | ROB的PC字段·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D28 | R29 | Int Dispatch/ROB | ROB的寄存器标识符字段·单比特翻转 | 单比特翻转 | F0 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D29 | R30 | Int Dispatch/ROB | ROB的寄存器标识符字段·双比特翻转 | 双比特翻转 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D30 | R31 | Int Dispatch/ROB | ROB的寄存器标识符字段·换值 | 换值（结构化） | F0 / F1 | —（不涉及事件触发） | CoreMark、GAP | 运行计数 |
| D31 | R32 | Int Dispatch/ROB | ROB的寄存器标识符字段·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D32 | R33 | Int Dispatch/ROB | ROB的done位·提前置位(固定间隔) | 状态（结构化）：提前置位 | F1 / F2 | —（不涉及事件触发） | CoreMark、ROB 填满核 | 运行计数 |
| D33 | R34 | Int Dispatch/ROB | ROB的done位·提前置位(事件触发) | 状态（结构化）：提前置位 | —（不适用固定间隔） | ROB 占用超过 80%（约 102/128 项） | ROB 填满核 | 事件覆盖计数 |
| D34 | R35 | Int Dispatch/ROB | ROB的done位·延迟置位(固定间隔) | 状态（结构化）：延迟置位 | F1 | —（不涉及事件触发） | ROB 填满核 | 运行计数 |
| D35 | R36 | Int Dispatch/ROB | ROB的done位·延迟置位(事件触发) | 状态（结构化）：延迟置位 | —（不适用固定间隔） | ROB 占用超过 80% | ROB 填满核 | 事件覆盖计数 |
| D36 | R37 | Int Dispatch/ROB | ROB的旧物理寄存器字段·单比特翻转 | 单比特翻转 | —（不适用固定间隔） | commit 阶段处理异常/分支误预测导致的 squash 时触发 | GAP | 事件覆盖计数 |
| D37 | R38 | Int Dispatch/ROB | ROB的旧物理寄存器字段·双比特翻转 | 双比特翻转 | —（不适用固定间隔） | commit 阶段处理异常/分支误预测导致的 squash 时触发 | GAP | 事件覆盖计数 |
| D38 | R39 | Int Dispatch/ROB | ROB的旧物理寄存器字段·换值 | 换值（结构化） | —（不适用固定间隔） | commit 阶段处理异常/分支误预测导致的 squash 时触发 | GAP | 事件覆盖计数 |
| D39 | R40 | Int Dispatch/ROB | ROB的旧物理寄存器字段·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | GAP | 事件覆盖计数 |
| D40 | R41 | Int Dispatch/ROB | ROB项整体·读到旧数据 | 错位拼接（结构化）：读到旧数据 | —（不适用固定间隔） | 该 ROB 槽位被新指令分派写入覆盖的瞬间 | CoreMark、Embench | 事件覆盖计数 |
| D41 | R42 | Int Dispatch/ROB | ROB头指针·单比特翻转 | 单比特翻转 | F0 | —（不涉及事件触发） | CoreMark | 运行计数 |
| D42 | R43 | Int Dispatch/ROB | ROB头指针·双比特翻转 | 双比特翻转 | F0 | —（不涉及事件触发） | CoreMark | 运行计数 |
| D43 | R44 | Int Dispatch/ROB | ROB头指针·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | CoreMark | 运行计数 |
| D44 | R45 | Int Dispatch/ROB | ROB尾指针·单比特翻转 | 单比特翻转 | F0 | —（不涉及事件触发） | CoreMark | 运行计数 |
| D45 | R46 | Int Dispatch/ROB | ROB尾指针·双比特翻转 | 双比特翻转 | F0 | —（不涉及事件触发） | CoreMark | 运行计数 |
| D46 | R47 | Int Dispatch/ROB | ROB尾指针·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | CoreMark | 运行计数 |
| D47 | R48 | Int Dispatch/ROB | Int IQ的ready位·提前置位 | 状态（结构化）：提前置位 | F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D48 | R49 | Int Dispatch/ROB | Int IQ的ready位·永不置位(固定间隔) | 状态（结构化）：永不置位 | F1 | —（不涉及事件触发） | ROB 填满核 | 运行计数 |
| D49 | R50 | Int Dispatch/ROB | Int IQ的ready位·永不置位(事件触发) | 状态（结构化）：永不置位 | —（不适用固定间隔） | IQ 占用超过 80%（约 51/64 项） | ROB 填满核 | 事件覆盖计数 |
| D50 | R51 | Int Dispatch/ROB | Int IQ的tag字段·换值 | 换值（结构化） | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D51 | R52 | Int Dispatch/ROB | Int IQ的tag字段·单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D52 | R53 | Int Dispatch/ROB | Int IQ的tag字段·双比特翻转 | 双比特翻转 | F0 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D53 | R54 | Int Dispatch/ROB | Int IQ的tag字段·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D54 | R55 | Int Dispatch/ROB | Int IQ的tag字段·读到旧数据 | 错位拼接（结构化）：读到旧数据 | —（不适用固定间隔） | 该 IQ 槽位被新 µop 写入覆盖的瞬间 | CoreMark、Embench | 事件覆盖计数 |
| D55 | R56 | Int Dispatch/ROB | 分发端口选择逻辑·状态翻转 | 状态（结构化） | F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 |
| D56 | R57 | FP/SIMD Decode | FP opcode字段·单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 |
| D57 | R58 | FP/SIMD Decode | FP opcode字段·双比特翻转 | 双比特翻转 | F0 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 |
| D58 | R59 | FP/SIMD Decode | FP opcode字段·换值 | 换值（结构化） | F0 / F1 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 |
| D59 | R60 | FP/SIMD Decode | V寄存器号字段·单比特翻转 | 单比特翻转 | F0 / F1 / F2 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 |
| D60 | R61 | FP/SIMD Decode | V寄存器号字段·双比特翻转 | 双比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 |
| D61 | R62 | FP/SIMD Decode | 指令路由判定位·状态翻转 | 状态（结构化） | F1 / F2 | —（不涉及事件触发） | Embench（含整数/浮点混合的程序） | 运行计数 |
| D62 | R63 | FP/SIMD Rename | 标量FP RAT映射字段·单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 |
| D63 | R64 | FP/SIMD Rename | 标量FP RAT映射字段·双比特翻转 | 双比特翻转 | F0 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 |
| D64 | R65 | FP/SIMD Rename | 标量FP RAT映射字段·换值 | 换值（结构化） | F0 / F1 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 |
| D65 | R66 | FP/SIMD Rename | 标量FP RAT映射字段·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 |
| D66 | R67 | FP/SIMD Rename | 标量FP RAT映射字段·读到旧数据 | 错位拼接（结构化）：读到旧数据 | —（不适用固定间隔） | 该表项被下一次重命名写入覆盖的瞬间 | PolyBench、Embench（浮点子集） | 事件覆盖计数 |
| D67 | R68 | FP/SIMD Rename | 向量RAT映射字段·单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 |
| D68 | R69 | FP/SIMD Rename | 向量RAT映射字段·双比特翻转 | 双比特翻转 | F0 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 |
| D69 | R70 | FP/SIMD Rename | 向量RAT映射字段·换值 | 换值（结构化） | F0 / F1 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 |
| D70 | R71 | FP/SIMD Rename | 向量RAT映射字段·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 |
| D71 | R72 | FP/SIMD Rename | 向量RAT映射字段·读到旧数据 | 错位拼接（结构化）：读到旧数据 | —（不适用固定间隔） | 该表项被下一次重命名写入覆盖的瞬间 | PolyBench、libjpeg-turbo | 事件覆盖计数 |
| D72 | R73 | FP/SIMD Rename | 标量FP空闲表·重复分配(固定间隔) | 状态（结构化）：重复分配 | F1 / F2 | —（不涉及事件触发） | 长依赖链压力核 | 运行计数 |
| D73 | R74 | FP/SIMD Rename | 标量FP空闲表·重复分配(事件触发) | 状态（结构化）：重复分配 | —（不适用固定间隔） | 标量池剩余项数低于阈值（建议 ≤12 项） | 长依赖链压力核 | 事件覆盖计数 |
| D74 | R75 | FP/SIMD Rename | 向量空闲表·重复分配(固定间隔) | 状态（结构化）：重复分配 | F1 / F2 | —（不涉及事件触发） | 长依赖链压力核（向量版：用向量累加链） | 运行计数 |
| D75 | R76 | FP/SIMD Rename | 向量空闲表·重复分配(事件触发) | 状态（结构化）：重复分配 | —（不适用固定间隔） | 向量池剩余项数低于阈值（建议 ≤6 项，按 48 项池容量等比例设置） | 长依赖链压力核（向量版：用向量累加链） | 事件覆盖计数 |
| D76 | R77 | FP/SIMD Rename | 标量FP空闲表·丢失释放 | 时序（结构化）：丢失释放 | F1（持续到运行结束） | —（不涉及事件触发） | PolyBench | 运行计数 |
| D77 | R78 | FP/SIMD Rename | 向量空闲表·丢失释放 | 时序（结构化）：丢失释放 | F1（持续到运行结束） | —（不涉及事件触发） | 长依赖链压力核（向量版） | 运行计数 |
| D78 | R79 | FP/SIMD Rename | SVE谓词RAT映射字段·单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench（SVE 向量化版） | 运行计数 |
| D79 | R80 | FP/SIMD Rename | SVE谓词RAT映射字段·双比特翻转 | 双比特翻转 | F0 | —（不涉及事件触发） | PolyBench（SVE 向量化版） | 运行计数 |
| D80 | R81 | FP/SIMD Rename | SVE谓词RAT映射字段·换值 | 换值（结构化） | F0 / F1 | —（不涉及事件触发） | PolyBench（SVE 向量化版） | 运行计数 |
| D81 | R82 | FP/SIMD Rename | SVE谓词RAT映射字段·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | PolyBench（SVE 向量化版） | 运行计数 |
| D82 | R83 | FP/SIMD Rename | SVE谓词RAT映射字段·读到旧数据 | 错位拼接（结构化）：读到旧数据 | —（不适用固定间隔） | 该表项被下一次重命名写入覆盖的瞬间 | PolyBench（SVE 向量化版） | 事件覆盖计数 |
| D83 | R84 | FP/SIMD Dispatch/ROB | ROB的PC字段(FP负载自检)·单比特翻转 | 单比特翻转 | F0 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 |
| D84 | R85 | FP/SIMD Dispatch/ROB | ROB的done位(FP负载自检，固定间隔)·提前置位 | 状态（结构化）：提前置位 | F1 / F2 | —（不涉及事件触发） | PolyBench、ROB 填满核（浮点版） | 运行计数 |
| D85 | R86 | FP/SIMD Dispatch/ROB | ROB的done位(FP负载自检，事件触发)·提前置位 | 状态（结构化）：提前置位 | —（不适用固定间隔） | ROB 占用超过 80% | ROB 填满核（浮点版） | 事件覆盖计数 |
| D86 | R87 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的ready位·提前置位 | 状态（结构化）：提前置位 | F1 / F2 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 |
| D87 | R88 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的tag字段·换值 | 换值（结构化） | F0 / F1 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 |
| D88 | R89 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的tag字段·单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 |
| D89 | R90 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的tag字段·双比特翻转 | 双比特翻转 | F0 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 |
| D90 | R91 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的tag字段·卡死 | 卡死（结构化） | F5 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 |
| D91 | R92 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的tag字段·读到旧数据 | 错位拼接（结构化）：读到旧数据 | —（不适用固定间隔） | 该 IQ 槽位被新 µop 写入覆盖的瞬间 | PolyBench、libjpeg-turbo | 事件覆盖计数 |

## Int Decode（10 行：D01–D10）

| ID | Excel行 | 单元 | 位置标签 | 注入位置 | 故障类型 | 故障模型（具体做法） | 适用频率 | 事件触发条件 | 适用负载 | 计数基准 | L1 重点观测点 | 预期结果与依据 | 设计理由 | 可对照文献 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| D01 | R2 | Int Decode | opcode字段·单比特翻转 | 译码输出锁存器（decode→rename 流水线寄存器）的 opcode 字段 | 单比特翻转 | 单比特翻转，比特与周期均匀随机 | F0 / F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 非法指令/非法寄存器号异常计数；squash 次数；IPC 偏差 | 预期多数变成非法指令→Crash，极少数翻到另一个合法 opcode 才可能 SDC，SDC 概率很低 | opcode 是控制字段，翻转后大概率不再是合法编码，直接触发非法指令异常；这一档作为「随机单比特翻转在译码级几乎测不出 SDC」的基线，呼应 Veritas 对控制逻辑的论断 | 无直接对照（Veritas 只有论断无实验；TC'23/IISWC'15 均未注入译码逻辑） |
| D02 | R3 | Int Decode | opcode字段·双比特翻转 | 译码输出锁存器（decode→rename 流水线寄存器）的 opcode 字段 | 双比特翻转 | 双比特翻转（模拟单粒子多位翻转 MBU：同一字段内两个比特同时翻转，位置随机，不要求相邻），周期均匀随机 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 同 「opcode字段·单比特翻转」，另外记录「双比特翻转后仍落在合法 opcode 编码空间」的比例，与 「opcode字段·单比特翻转」 的单比特命中率对照 | 预期崩溃率高于 「opcode字段·单比特翻转」：两个比特同时错，落在合法编码空间的概率通常低于单比特翻转（合法编码是稀疏子集，出错维度越多越难蒙对） | CHAOS 论文的多比特实验发现，寄存器/L1 这类结构在多比特翻转下崩溃率会进一步走高；此行验证同一规律是否在译码字段上成立 | 可与 CHAOS 论文的多比特结果做规律性类比（非直接对照，CHAOS 未注入译码逻辑） |
| D03 | R4 | Int Decode | opcode字段·换值 | 译码输出锁存器的 opcode 字段 | 换值（结构化） | 把 opcode 强制替换成 ISA 中另一个合法、且操作数格式兼容的 opcode（如同为整数 ALU 类指令间互换） | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 指令替换（TC'23 分类里的 Instruction Replacement）发生率；替换后是否仍能通过后续流水线不触发异常 | 预期 SDC 概率明显高于 「opcode字段·单比特翻转」/「opcode字段·双比特翻转」 的翻转，因为绕开了「非法编码即崩溃」这道天然防线 | 直接检验 Veritas「控制逻辑故障极不可能导致 SDC」这条论断——论断成立的前提是故障产生非法状态，这里刻意构造合法状态来测试论断的边界 | 无（同上，本方案首次覆盖） |
| D04 | R5 | Int Decode | 源/目的寄存器号字段·单比特翻转 | 译码输出锁存器的源/目的寄存器操作数字段 | 单比特翻转 | 单比特翻转 | F0 / F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 操作数强制切换（Operand Forced Switch）发生率；寄存器号越界异常计数 | 寄存器号字段位宽窄、合法值密集（31 个架构寄存器只需 5 位编码），翻转后仍落在合法范围的概率较高，预期比 opcode 翻转更容易产生「合法但错误」的 SDC | 寄存器号字段是译码阶段里结构化故障和随机翻转预期差异最小的位置，适合做二者的对照实验 | 无 |
| D05 | R6 | Int Decode | 源/目的寄存器号字段·双比特翻转 | 译码输出锁存器的源/目的寄存器操作数字段 | 双比特翻转 | 双比特翻转，位置随机 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 同 「源/目的寄存器号字段·单比特翻转」，另外对比双比特翻转后仍落在合法寄存器号范围的比例 | 5 位编码的字段翻转 2 位后落在 0~30 合法范围的概率明显低于翻转 1 位，预期崩溃率高于 「源/目的寄存器号字段·单比特翻转」 | 与 「opcode字段·单比特翻转」/「opcode字段·双比特翻转」 的对比逻辑一致，用来确认「字段位宽越窄，多比特翻转对合法性的破坏越明显」这一推论在不同字段上是否成立 | 无 |
| D06 | R7 | Int Decode | 立即数字段·单比特翻转 | 译码输出锁存器的立即数字段 | 单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | Embench | 运行计数 | 立即数参与地址计算/分支目标计算的比例；数据损坏（Data Corruption）占比 | 立即数翻转不改变指令类型，几乎必然通过流水线；预期是 Int Decode 里单比特翻转 SDC 概率最高的字段 | 立即数没有「合法性」这道门槛（任意位模式都是合法立即数），是 Veritas 论断里「控制逻辑不易 SDC」这条规律的例外情形，值得单独验证 | 无 |
| D07 | R8 | Int Decode | 立即数字段·双比特翻转 | 译码输出锁存器的立即数字段 | 双比特翻转 | 双比特翻转，位置随机 | F0 / F1 | —（不涉及事件触发） | Embench | 运行计数 | 同 「立即数字段·单比特翻转」 | 立即数字段没有合法性门槛，预期双比特翻转的 SDC 概率和单比特翻转（「立即数字段·单比特翻转」）接近，不会像 opcode/寄存器号那样出现明显的崩溃率跃升——这是本方案里用来验证「位宽越窄越敏感」这条推论的反例对照 | 立即数是「无合法性约束字段」的代表，和「opcode字段」「源/目的寄存器号字段」这类「有合法性约束字段」形成系统对比，检验多比特翻转的效应是否只在有约束的字段上才明显 | 无 |
| D08 | R9 | Int Decode | 立即数字段·符号扩展位定向翻转 | 译码输出锁存器的立即数字段——不是随机选位，是精确翻转「立即数完成符号扩展后的最高位（符号位）」这一根线 | 状态（结构化） | 不做随机比特翻转，直接翻转符号扩展后立即数的符号位，模拟符号扩展硬件本身的单点故障；同一次注入只翻这一位，不叠加其它随机位 | F0 / F1 | —（不涉及事件触发） | Embench、GAP | 运行计数 | 地址计算结果相对无故障基线的偏差幅度（是否发生数量级跳变）；分支目标计算错误发生率；访存越界异常计数 | 对地址类指令（load/store 偏移、ADR/ADRP）符号翻转会让目标地址产生巨大跳变，预期多数直接越界→Crash；对小整数常量场景（如循环计数初值、比较阈值）符号翻转只是让数值变号，不一定立刻崩溃，预期在这类场景下产生「数值方向错但仍在合法范围」的隐蔽 SDC，是本方案预期两极分化最明显的一行 | 「立即数字段·单比特翻转」「立即数字段·双比特翻转」两行翻的是「字段里随便一位」，翻到符号位只是众多可能之一，概率被字段宽度稀释掉了，测不出符号扩展这个具体机制的专属风险；这行直接把故障打在符号扩展逻辑上，是针对 Decode 真实在做的「符号扩展」这项工作设计的结构化模型，不是泛化的比特翻转 | 无 |
| D09 | R10 | Int Decode | 立即数字段·子字段拼接错位 | 译码输出锁存器的立即数字段——模拟「把立即数的某个编码子字段拼到了错误的位移位置」这一类拼装逻辑故障，而不是某个比特被打翻 | 错位拼接（结构化）：子字段移位错位 | 把正确拼装的立即数替换成「子字段顺序或移位量算错后拼出来的另一个合法数值」，例如该放到 imm[19:16] 位置的子字段被错误地拼到了 imm[15:12] 的位置——这不是把整个字段换成另一个无关的合法值（换值），是组成这个值的碎片本身被装错了位置 | F0 / F1 | —（不涉及事件触发） | Embench、GAP | 运行计数 | 拼装后立即数与正确值的偏差模式（是否表现为规律性的移位/换位，而不是随机噪声）；GAP 的数组下标偏移对这类拼接错误的敏感度 | 预期是立即数相关三行里 SDC 概率最高的一行——拼装结果本身就是「合法值」，没有任何门槛可以拦截，且错位模式保留了原始比特的组成部分（只是位置错了），比纯数值错误更难被察觉 | 对应 ARM64 立即数编码常见的跨子字段拼装、按指令类不同移位/符号扩展的事实（ADD/SUB 12 位、MOVZ 16 位、条件分支 19 位、无条件分支 26 位、ADRP 21 位，位宽各不相同），这是 Int Decode 单元真正在做的工作，随机比特翻转完全没有针对性覆盖这个维度；这行原来误标成「换值」，审查后改正为「错位拼接」——换值是整个字段换成另一个无关的合法值，这里是拼装这个值的碎片本身装错了位置，机制不同，不能混用同一个类型标签 | 无 |
| D10 | R11 | Int Decode | 裂解控制位·状态翻转 | 指令裂解控制位（该指令是否需要在 dispatch 级拆分成多个 µop 的判定，如 LDP/STP）——该字段本身为单比特控制位，无多比特版本 | 状态（结构化） | 强制翻转裂解判定位：该拆的指令不拆（数据不全）／不该拆的指令被拆（产生多余 µop） | F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | µop 数量与无故障基线的偏差；load/store 完整性异常 | 「不该拆的指令被拆」大概率产生非法 µop 序列→Crash；「该拆的指令不拆」可能导致 LDP/STP 类指令只搬一半数据，预期是这个位置里最可能产生 SDC 的模型 | 这是 Decode 专属的结构化功能故障，对应 A72 公开资料里确认的「宏操作裂解」机制，没有对应的真实翻转位置可比，只能用功能级建模 | 无（结构化模型，无可比翻转基线） |

## Int Rename（14 行：D11–D24）

| ID | Excel行 | 单元 | 位置标签 | 注入位置 | 故障类型 | 故障模型（具体做法） | 适用频率 | 事件触发条件 | 适用负载 | 计数基准 | L1 重点观测点 | 预期结果与依据 | 设计理由 | 可对照文献 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| D11 | R12 | Int Rename | RAT映射字段·单比特翻转 | RAT（架构→物理映射表）表项：某架构寄存器当前映射到的物理寄存器号字段 | 单比特翻转 | 单比特翻转 | F0 / F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 影子 RAT 与实际 RAT 的分歧项数；依赖检查失败次数 | 预期大概率翻到「当前未分配」的物理寄存器号，命中依赖检查失败→Crash，SDC 概率很低，类似 TC'23 对 ROB 字段翻转测出的 0% | 作为 「RAT映射字段·换值(固定间隔)」 结构化模型的对照基线：先验证单比特随机翻转在 gem5 依赖检查机制下确实测不出 SDC，才能说明 「RAT映射字段·换值(固定间隔)」 的「换值」设计是必要的 | 可与 TC'23 的 ROB 翻转结果做同机制类比（非直接对照，TC'23 未注入 RAT） |
| D12 | R13 | Int Rename | RAT映射字段·双比特翻转 | RAT 表项：某架构寄存器当前映射到的物理寄存器号字段 | 双比特翻转 | 双比特翻转，位置随机 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 同 「RAT映射字段·单比特翻转」 | 128 项物理寄存器需要 7 位编码，双比特翻转后落在「已分配」区间从而绕开依赖检查的概率高于单比特翻转，预期 SDC 概率略高于 「RAT映射字段·单比特翻转」，但仍显著低于 「RAT映射字段·换值(固定间隔)」 的结构化模型 | 检验「多比特翻转是否本身就能一定程度绕开依赖检查」，和 「RAT映射字段·换值(固定间隔)」 的「刻意构造绕开」形成强度对照，帮助判断 「RAT映射字段·换值(固定间隔)」 这类结构化模型带来的增量到底有多大 | 无 |
| D13 | R14 | Int Rename | RAT映射字段·换值(固定间隔) | RAT 表项：某架构寄存器当前映射到的物理寄存器号字段 | 换值（结构化） | 把该架构寄存器的映射，强制改成「另一条当前仍在飞的指令也在用」的物理寄存器号（从 ROB 里随机挑一个活跃物理寄存器号写入） | F0 / F1 | —（不涉及事件触发） | CoreMark、分支误预测密集核 | 运行计数 | 污染物理寄存器被后续几条指令读取的扇出数；数据损坏与操作数强制切换的比例 | 新映射合法（该物理寄存器确实存在且在用），预期能绕开依赖检查，产生真正的 SDC——这是本方案对总表里「Int Rename 标红但无实验数据」这一矛盾的直接回应 | 总表判定依据写「RAT 无保护，翻转会让指令读写错误的物理寄存器，结果被静默提交」，但迄今没有实验验证这个判断；「RAT映射字段·单比特翻转」/「RAT映射字段·双比特翻转」 已证明随机翻转测不出来，此行专门用结构化模型验证判断本身对不对；固定间隔只按 F0/F1 盲打，命中「误预测正在恢复 RAT」这个窗口纯属随缘——精确瞄准这个窗口的版本单独拆成了「RAT映射字段·换值(误预测事件触发)」一行，两行不重叠，避免同一个实验条件被算两次、还套用两套不同的样本量口径 | 无（本方案首次设计并预期填补这一空白） |
| D14 | R15 | Int Rename | RAT映射字段·换值(误预测事件触发) | RAT 表项：某架构寄存器当前映射到的物理寄存器号字段 | 换值（结构化，事件触发版） | 同 「RAT映射字段·换值(固定间隔)」，但只在分支误预测恢复的瞬间触发注入，而不是固定间隔 | —（不适用固定间隔） | 误预测恢复瞬间 | 分支误预测密集核 | 事件覆盖计数 | 同 「RAT映射字段·换值(固定间隔)」；额外记录误预测恢复路径本身是否因为这次注入而进一步出错（双重故障效应） | 预期比 「RAT映射字段·换值(固定间隔)」 固定间隔版更高效地命中「RAT 正在被大量重写」的窗口，SDC 命中率预期不低于 「RAT映射字段·换值(固定间隔)」 的固定间隔版 | 「RAT映射字段·换值(固定间隔)」 用固定间隔注入已经能覆盖这个场景，但命中「RAT 正被恢复」这个窗口纯属运气；此行单独用事件触发复现同一模型，检验事件触发是否真的比固定间隔更有效率，是本方案「按事件触发设计是否有意义」这条方法论主张的直接验证 | 无 |
| D15 | R16 | Int Rename | RAT映射字段·卡死 | RAT（架构→物理映射表）表项：某架构寄存器当前映射到的物理寄存器号字段 | 卡死（结构化） | 运行开始时随机选一个 RAT 表项的一个比特位置，永久固定为 0 或 1（stuck-at-0/stuck-at-1 各半概率），只要该架构寄存器被重命名过，这一位就持续带着缺陷，直到该表项被下一次重命名覆盖为止 | F5 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 该 RAT 项被覆盖前累计参与重命名的次数（卡死影响的「曝光次数」）；影子 RAT 与实际 RAT 的持续分歧模式（是否表现为固定偏差而不是随机噪声） | 卡死和瞬态翻转面对的是同一道依赖检查机制，此前没有实验数据能证明持续性本身会显著改变 SDC 概率的期望值，所以按最坏情形分配样本，不像「RAT映射字段·换值(固定间隔)」那样有方向性预期；但持久性缺陷会让同一个错误反复影响多条指令而不是只错一次，预期「污染指令数」这个量会明显高于「RAT映射字段·单比特翻转」/「RAT映射字段·双比特翻转」，即使 SDC 概率本身不一定更高 | RAT 表项是被反复读写的存储结构（架构寄存器每次被重命名都要查一次），不是每周期整体刷新的译码锁存器，适合卡死模型；边际缺陷相关文献（Veritas、DATE'25）的核心论点是持久性故障是现场 SDC 的主要根因，这是本方案审查后新增的一类故障类型，此前 59 行矩阵里完全没有覆盖 | 可与 CHAOS 论文的 stuck-at-0/1 模型做机制类比（CHAOS 对象是 RISC-V 架构寄存器，非 RAT 结构，非直接对照） |
| D16 | R17 | Int Rename | RAT映射字段·读到旧数据 | RAT（架构→物理映射表）表项：某架构寄存器当前映射到的物理寄存器号字段 | 错位拼接（结构化）：读到旧数据 | 在该表项即将被下一次重命名写入覆盖的那个周期，注入让这次写入静默失效，表项保留上一次占用者（几条指令之前）的旧映射，直到再下一次重命名尝试才真正更新——不是把值换成另一个活跃寄存器号（换值），也不是某一位永久错（卡死），是「本该发生的更新没有真正发生」在数据层面的体现 | —（不适用固定间隔） | 该 RAT 表项被下一次重命名写入覆盖的瞬间 | CoreMark、Embench | 事件覆盖计数 | 该表项的写入尝试次数与实际生效次数的差值；使用旧映射的下游指令读到的数据与真正期望值的偏差 | 旧映射本身是合法的（几条指令之前确实用过），不会触发任何检查失败——这是本方案里预期 SDC 概率最高的几行之一，因为它绕开的不是某道逻辑检查，而是「数据到底新不新」这件事本身没有检查手段去确认 | 「RAT映射字段·换值(固定间隔)」换的是当前活跃寄存器，「RAT映射字段·卡死」错的是固定一位，这行错的是整条映射「过时」——三者是三种不同的物理故障根源（逻辑故障/存储位缺陷/写入时序故障），审查全篇故障类型覆盖后发现「读到旧数据」这类机制完全缺失，RAT 是这个项目里被反复写读得最频繁的结构之一，补在这里价值最大 | 可与 MICRO'24 DelayAVF 的机制类比：该论文描述字线延迟导致读操作实际锁存的是上一个地址的旧数据，这份旧数据本身是完好的、校验配套的合法数据，ECC 检查完全通过；但 DelayAVF 针对的是 Ibex 寄存器堆的时延故障模型，不是 gem5 里的功能级注入，是机制类比，不是直接对照 |
| D17 | R18 | Int Rename | 空闲表·重复分配(固定间隔) | Free List（空闲物理寄存器表）队列项 | 状态（结构化）：重复分配 | 把某一项强制标记为「仍空闲」，即使它已被分配出去，导致下次 rename 时被分配给另一条指令，两条指令共享同一物理寄存器（WAW 冲突） | F1 / F2 | —（不涉及事件触发） | CoreMark、长依赖链压力核 | 运行计数 | 重复分配计数；两条指令写同一物理寄存器后，先写的值是否被静默覆盖且未被检测 | 预期 SDC 与 Crash 并存：如果两条指令的写入顺序恰好被后续读取按程序序正确消费，可能被掩蔽；顺序错乱则产生数据损坏类 SDC | 空闲表项数少（跟随 numPhysIntRegs=128 的分配粒度），事件触发能显著提高故障命中「正在被追踪」项的概率，比固定频率盲注更有效率 | 无 |
| D18 | R19 | Int Rename | 空闲表·重复分配(事件触发) | Free List（空闲物理寄存器表）队列项 | 状态（结构化）：重复分配 | 把某一项强制标记为「仍空闲」，即使它已被分配出去，导致下次 rename 时被分配给另一条指令，两条指令共享同一物理寄存器（WAW 冲突） | —（不适用固定间隔） | 空闲表剩余项数低于阈值（建议剩余 ≤8 项时） | 长依赖链压力核 | 事件覆盖计数 | 重复分配计数；两条指令写同一物理寄存器后，先写的值是否被静默覆盖且未被检测 | 预期 SDC 与 Crash 并存：如果两条指令的写入顺序恰好被后续读取按程序序正确消费，可能被掩蔽；顺序错乱则产生数据损坏类 SDC；精确瞄准「空闲表快见底」这个窗口，预期比固定间隔版更高效地命中重复分配场景 | 空闲表项数少（跟随 numPhysIntRegs=128 的分配粒度），事件触发能显著提高故障命中「正在被追踪」项的概率，比固定频率盲注更有效率；固定间隔版已经能覆盖这个场景，但命中「空闲表恰好快满」这个窗口纯属随缘；这行精确瞄准该窗口，检验事件触发是否真的比固定间隔更有效率，和「RAT映射字段·换值(误预测事件触发)」是同一条方法论主张的另一处验证 | 无 |
| D19 | R20 | Int Rename | 空闲表·丢失释放 | Free List 队列项 | 时序（结构化）：丢失释放 | 某物理寄存器该被释放回空闲表时，注入让这次释放事件被抑制（该发生的事件没发生），空闲表逐渐缩水 | F1（持续到运行结束，属于一次性触发后持续影响） | —（不涉及事件触发） | Embench、长依赖链压力核 | 运行计数 | 空闲表尺寸随时间的下降趋势；rename 阶段停顿周期数 | 预期主导结果是 Timeout（空闲表耗尽后 rename 长期阻塞），SDC 概率很低——这个位置的价值在「前兆」：停顿周期数会在最终结局前明显上升，是本方案要验证的一种 SDC/Timeout 前兆特征 | 对应「统一故障类型」表里的时序类（丢失一次事件）；专门设计来验证 L1 观测层（rename 停顿计数）能不能在结局发生前给出预警信号，产出是一条趋势曲线而不是 SDC 比例 | 无 |
| D20 | R21 | Int Rename | 空闲表头/尾指针·单比特翻转 | Free List 头/尾指针 | 单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | CoreMark | 运行计数 | 指针错位后首次异常分配发生的位置（重复分配 vs 分配到未初始化项） | 介于 「RAT映射字段·单比特翻转」 和 「空闲表·重复分配(固定间隔)」（重复分配）效应之间，预期结果分布是二者的混合 | 指针翻转会同时引发「可能重复分配」和「可能分配到脏项」两种效应，用来检验单比特翻转能否触达 「空闲表·重复分配(固定间隔)」 结构化模型要专门构造才能命中的场景 | 无 |
| D21 | R22 | Int Rename | 空闲表头/尾指针·双比特翻转 | Free List 头/尾指针 | 双比特翻转 | 双比特翻转，位置随机 | F0 | —（不涉及事件触发） | CoreMark | 运行计数 | 同 「空闲表头/尾指针·单比特翻转」 | 指针字段位宽窄（128 项对应 7 位），双比特翻转造成的偏移量通常更大，预期比 「空闲表头/尾指针·单比特翻转」 更容易越出合法索引范围而触发崩溃，SDC 概率不一定高于 「空闲表头/尾指针·单比特翻转」 | 与 「RAT映射字段·单比特翻转」/「RAT映射字段·双比特翻转」、「源/目的寄存器号字段·单比特翻转」/「源/目的寄存器号字段·双比特翻转」 同一逻辑在指针字段上的复现，补齐「窄位宽字段的多比特翻转效应」这条系统对比线 | 无 |
| D22 | R23 | Int Rename | 空闲表头/尾指针·卡死 | Free List 头/尾指针 | 卡死（结构化） | 运行开始时随机选指针寄存器的一个比特位置，永久固定为 0 或 1 | F5 | —（不涉及事件触发） | CoreMark、长依赖链压力核 | 运行计数 | 指针实际值与期望值的偏差是否表现为固定模式（如恒偏移某个量）；重复分配/越界分配的持续发生率 | 卡死一位会让指针的移动轨迹产生系统性的固定偏差（比如永远跳过某个区间、或在某个范围内循环），比瞬态翻转（「空闲表头/尾指针·单比特翻转」/「空闲表头/尾指针·双比特翻转」）更容易造成持续性的重复分配或越界，预期 SDC/Crash 的时间分布上更集中，但没有强先验说明总体概率会明显偏离翻转版本 | 指针是每次分配/释放都要读写的存储结构，符合卡死模型的适用条件；和空闲表的「空闲表·重复分配(固定间隔)」「空闲表·丢失释放」两个结构化模型是不同角度——那两个模拟的是具体的逻辑行为错误，这行模拟的是物理存储介质的持久性缺陷 | 无 |
| D23 | R24 | Int Rename | 重命名检查点·单比特翻转 | 重命名恢复检查点（若 gem5 用 checkpoint 机制保存 RAT 快照；具体机制需先核实 src/cpu/o3/rename.cc 再定） | 单比特翻转 | 单比特翻转，仅在检查点存在期间注入 | —（不适用固定间隔） | 检查点建立后到被消费（误预测恢复或释放）之间的窗口内 | 分支误预测密集核 | 事件覆盖计数 | 误预测恢复后 RAT 与影子 RAT 的分歧 | 如果 gem5 确实用 checkpoint 机制，预期这类故障只在误预测发生时才暴露，平时完全沉默——是本方案里「潜伏期最长」的候选位置之一 | 机制未最终确认（见《OoO单元参数总表.md》单元 2 的说明），这里先按「可能存在检查点」设计，实验前必须先读 gem5 源码确认，如果不是 checkpoint 机制，这一行的位置定义要相应调整，不能直接套用 | 无 |
| D24 | R25 | Int Rename | 重命名检查点·双比特翻转 | 重命名恢复检查点 | 双比特翻转 | 双比特翻转，位置随机，仅在检查点存在期间注入 | —（不适用固定间隔） | 检查点建立后到被消费之间的窗口内 | 分支误预测密集核 | 事件覆盖计数 | 同 「重命名检查点·单比特翻转」 | 同 「重命名检查点·单比特翻转」 的逻辑，双比特版本用来检验检查点字段在多位同时错误时是否更容易绕过潜在的一致性检查（如果 gem5 实现里有类似机制） | 与 「重命名检查点·单比特翻转」 配对，机制确认前先按占位设计，实验前需同步核实 | 无 |

## Int Dispatch/ROB（31 行：D25–D55）

| ID | Excel行 | 单元 | 位置标签 | 注入位置 | 故障类型 | 故障模型（具体做法） | 适用频率 | 事件触发条件 | 适用负载 | 计数基准 | L1 重点观测点 | 预期结果与依据 | 设计理由 | 可对照文献 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| D25 | R26 | Int Dispatch/ROB | ROB的PC字段·单比特翻转 | ROB 项的 PC 字段 | 单比特翻转 | 单比特翻转 | F0 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 依赖检查失败次数；commit 前 squash 次数 | 预期复现 TC'23 结果：SDC≈0%，几乎全部在 commit 前触发依赖检查失败→Crash | 复现已发表结果的基线格，用来验证本方案注入器实现是否正确；需注意 TC'23 原始方法是对整个 ROB 项随机选 1 位注入（PC 和寄存器标识符等子字段都可能被命中），本行是精神上对应、字段级细化的复现，不是逐比特协议复刻 | TC'23：ROB 翻转 SDC 概率 0.0%（图 3），Armv7/Armv8 一致 |
| D26 | R27 | Int Dispatch/ROB | ROB的PC字段·双比特翻转 | ROB 项的 PC 字段 | 双比特翻转 | 双比特翻转，位置随机 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 同 「ROB的PC字段·单比特翻转」 | TC'23 未测过双比特版本，没有对照数字，预期方向同 「ROB的PC字段·单比特翻转」（依赖检查依然会拦截大部分情形），但双比特是否存在「两个比特恰好凑成另一个合法 PC」的边缘情形需要实验验证 | PC 字段位宽较大（约 48 位地址空间），双比特翻转命中「仍是合法且被使用的地址」的概率理论上比窄字段更低，这里用来验证宽字段是否天然比窄字段更抗多比特翻转 | 无（TC'23 只做单比特，此为本方案的探索性延伸） |
| D27 | R28 | Int Dispatch/ROB | ROB的PC字段·卡死 | ROB 项的 PC 字段 | 卡死（结构化） | 运行开始时随机选一个比特位置，永久固定为 0 或 1，该 ROB 项存活期间持续带着这个缺陷 | F5 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 同「ROB的PC字段·单比特翻转」，另外记录该 ROB 项在整个存活期间是否被反复读取到同一个错误值 | 依赖检查逐次读取时都会生效，不会因为错误从瞬态变成持续而失效，预期这行的 SDC 概率仍然接近「ROB的PC字段·单比特翻转」的低概率结果——这行的价值主要是确认「依赖检查对持久性故障同样有效」这个论断，不是预期挖出新的 SDC | 补齐 ROB 存储型字段的卡死覆盖，和「ROB的寄存器标识符字段·卡死」构成基线字段的卡死对照 | 无（可与基线「ROB的PC字段·单比特翻转」对比，检验持久性是否改变 TC'23 式的 SDC≈0% 结论） |
| D28 | R29 | Int Dispatch/ROB | ROB的寄存器标识符字段·单比特翻转 | ROB 项的目的寄存器标识符字段（架构或物理号） | 单比特翻转 | 单比特翻转 | F0 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 同 「ROB的PC字段·单比特翻转」 | 预期复现 TC'23 结果：SDC≈0% | 同 「ROB的PC字段·单比特翻转」，两个基线格共同构成注入器正确性验证的对照组 | TC'23：ROB 翻转 SDC 概率 0.0% |
| D29 | R30 | Int Dispatch/ROB | ROB的寄存器标识符字段·双比特翻转 | ROB 项的目的寄存器标识符字段 | 双比特翻转 | 双比特翻转，位置随机 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 同 「ROB的PC字段·单比特翻转」 | 寄存器标识符字段窄（7 位），双比特翻转后仍落在合法已分配范围的概率高于 PC 字段的 「ROB的PC字段·双比特翻转」，预期 SDC 概率略高于 「ROB的PC字段·双比特翻转」，可与之做窄字段/宽字段的横向对比 | 与 「ROB的PC字段·双比特翻转」 构成「同为双比特翻转，字段宽窄不同」的对照组，帮助判断 「RAT映射字段·双比特翻转」、「空闲表头/尾指针·双比特翻转」 等其它窄字段的预期是否合理 | 无 |
| D30 | R31 | Int Dispatch/ROB | ROB的寄存器标识符字段·换值 | ROB 项的目的寄存器标识符字段 | 换值（结构化） | 改成另一个当前活跃的合法物理寄存器号（同 Int Rename 的 「RAT映射字段·换值(固定间隔)」 思路，在 ROB 层面复现） | F0 / F1 | —（不涉及事件触发） | CoreMark、GAP | 运行计数 | 污染扇出数；commit 后该 ROB 项对应指令是否被判定为正常完成 | 预期 SDC 概率明显高于 「ROB的PC字段·单比特翻转」/「ROB的寄存器标识符字段·单比特翻转」，因为绕开了依赖检查——和 「RAT映射字段·换值(固定间隔)」 一起构成「Rename 和 ROB 两层都验证同一个绕过机制」的交叉证据 | 「ROB的PC字段·单比特翻转」「ROB的寄存器标识符字段·单比特翻转」 已确认随机翻转测不出 SDC，此行验证「换值绕过依赖检查」这个结构化思路在 ROB 层同样有效，与单元 2 的 「RAT映射字段·换值(固定间隔)」 互为印证 | 无（结构化模型，「ROB的PC字段·单比特翻转」「ROB的寄存器标识符字段·单比特翻转」 是同源对照） |
| D31 | R32 | Int Dispatch/ROB | ROB的寄存器标识符字段·卡死 | ROB 项的目的寄存器标识符字段（架构或物理号） | 卡死（结构化） | 运行开始时随机选一个比特位置，永久固定为 0 或 1 | F5 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 同「ROB的寄存器标识符字段·单比特翻转」 | 同「ROB的PC字段·卡死」的逻辑，预期 SDC 概率仍然接近对应翻转版本的低概率结果 | 补齐 ROB 存储型字段的卡死覆盖 | 无（可与基线「ROB的寄存器标识符字段·单比特翻转」对比） |
| D32 | R33 | Int Dispatch/ROB | ROB的done位·提前置位(固定间隔) | ROB 项的 done/completed 状态位——该字段本身为单比特控制位，无多比特版本 | 状态（结构化）：提前置位 | 指令还未真正执行完就把 done 位提前标记为 1，导致 commit 阶段读到未定义或旧的执行结果 | F1 / F2 | —（不涉及事件触发） | CoreMark、ROB 填满核 | 运行计数 | commit 读到的结果与真实执行结果的偏差；该指令下游消费者读到错误值的比例 | 预期是 Int Dispatch/ROB 里 SDC 概率最高的位置：done 位不参与依赖检查（依赖检查看的是寄存器号是否可用，不看指令是否真的算完），预期能大量产生「合法但结果错」的 SDC | TC'23 从未注入过状态位，只注入了 PC 和寄存器标识符；总表判定「ROB 无保护，翻转会破坏提交顺序或结果，可静默提交」的依据主要就是这类状态位，此行是对该判断的直接实验验证 | 无（本方案对现有文献的主要补充点之一） |
| D33 | R34 | Int Dispatch/ROB | ROB的done位·提前置位(事件触发) | ROB 项的 done/completed 状态位——该字段本身为单比特控制位，无多比特版本 | 状态（结构化）：提前置位 | 指令还未真正执行完就把 done 位提前标记为 1，导致 commit 阶段读到未定义或旧的执行结果 | —（不适用固定间隔） | ROB 占用超过 80%（约 102/128 项） | ROB 填满核 | 事件覆盖计数 | commit 读到的结果与真实执行结果的偏差；该指令下游消费者读到错误值的比例 | 预期是 Int Dispatch/ROB 里 SDC 概率最高的位置：done 位不参与依赖检查（依赖检查看的是寄存器号是否可用，不看指令是否真的算完），预期能大量产生「合法但结果错」的 SDC；精确瞄准「ROB 快满」这个窗口，预期比固定间隔版更高效地命中该场景 | TC'23 从未注入过状态位，只注入了 PC 和寄存器标识符；总表判定「ROB 无保护，翻转会破坏提交顺序或结果，可静默提交」的依据主要就是这类状态位，此行是对该判断的直接实验验证；固定间隔版已经能覆盖这个场景，但命中「ROB 恰好快满」这个窗口纯属随缘；这行精确瞄准该窗口，检验事件触发是否真的比固定间隔更有效率 | 无（本方案对现有文献的主要补充点之一） |
| D34 | R35 | Int Dispatch/ROB | ROB的done位·延迟置位(固定间隔) | ROB 项的 done/completed 状态位——该字段本身为单比特控制位，无多比特版本 | 状态（结构化）：延迟置位 | 已完成的指令不被标记为 done，导致该项长期占用 ROB 无法提交 | F1 | —（不涉及事件触发） | ROB 填满核 | 运行计数 | commit 阻塞持续时间；ROB 占用率随时间的变化曲线 | 预期主导结果是 Timeout（ROB 被单个卡住的项堵死，后续全部无法提交），SDC 概率很低 | 和 「空闲表·丢失释放」（Free List 泄漏）同类，用来验证「资源耗尽型」故障的前兆特征：ROB 占用率应该在 Timeout 判定前持续处于高位，检验这个信号能否提前预警，产出是趋势曲线 | 无 |
| D35 | R36 | Int Dispatch/ROB | ROB的done位·延迟置位(事件触发) | ROB 项的 done/completed 状态位——该字段本身为单比特控制位，无多比特版本 | 状态（结构化）：延迟置位 | 已完成的指令不被标记为 done，导致该项长期占用 ROB 无法提交 | —（不适用固定间隔） | ROB 占用超过 80% | ROB 填满核 | 事件覆盖计数 | commit 阻塞持续时间；ROB 占用率随时间的变化曲线 | 预期主导结果是 Timeout（ROB 被单个卡住的项堵死，后续全部无法提交），SDC 概率很低；精确瞄准「ROB 快满」这个窗口，预期比固定间隔版更高效地命中该场景 | 和 「空闲表·丢失释放」（Free List 泄漏）同类，用来验证「资源耗尽型」故障的前兆特征：ROB 占用率应该在 Timeout 判定前持续处于高位，检验这个信号能否提前预警，产出是趋势曲线；固定间隔版已经能覆盖这个场景，但命中「ROB 恰好快满」这个窗口纯属随缘；这行精确瞄准该窗口 | 无 |
| D36 | R37 | Int Dispatch/ROB | ROB的旧物理寄存器字段·单比特翻转 | ROB 项的 old-physical-register 字段（squash 回滚要用） | 单比特翻转 | 单比特翻转，仅在该 ROB 项存活期间注入 | —（不适用固定间隔） | commit 阶段处理异常/分支误预测导致的 squash 时触发 | GAP | 事件覆盖计数 | squash 恢复后架构寄存器状态与黄金参照的分歧 | 平时注入无效果（该字段只在 squash 路径上被读取），预期是本方案里潜伏期最长的位置之一；一旦命中 squash 事件，预期能直接污染回滚后的架构状态，SDC 概率较高 | 这是「沉默的定时炸弹」型故障的典型位置：F0~F3 固定频率盲注命中概率极低，必须用事件触发才有效率，用来检验「按事件触发」这种频率设计思路本身是否比固定间隔更有效 | 无（结构化程度依赖具体命中的字段位，先按普通翻转建模，具体是否需要「换值」版本待单比特/双比特结果出来后再决定） |
| D37 | R38 | Int Dispatch/ROB | ROB的旧物理寄存器字段·双比特翻转 | ROB 项的 old-physical-register 字段 | 双比特翻转 | 双比特翻转，位置随机，仅在该 ROB 项存活期间注入 | —（不适用固定间隔） | commit 阶段处理异常/分支误预测导致的 squash 时触发 | GAP | 事件覆盖计数 | 同 「ROB的旧物理寄存器字段·单比特翻转」 | 同 「ROB的旧物理寄存器字段·单比特翻转」 逻辑，双比特版本用于对照该字段的窄字段（7 位物理寄存器号）多比特敏感性 | 与 「ROB的旧物理寄存器字段·单比特翻转」 配对，补齐这一事件触发位置的单/双比特对照 | 无 |
| D38 | R39 | Int Dispatch/ROB | ROB的旧物理寄存器字段·换值 | ROB 项的 old-physical-register 字段（squash 回滚要用） | 换值（结构化） | 把该字段改成另一个当前活跃的合法物理寄存器号（同「RAT映射字段·换值(固定间隔)」「ROB的寄存器标识符字段·换值」的思路），仅在该 ROB 项存活期间注入 | —（不适用固定间隔） | commit 阶段处理异常/分支误预测导致的 squash 时触发 | GAP | 事件覆盖计数 | squash 恢复后架构寄存器状态与黄金参照的分歧；被错误释放/占用的物理寄存器后续读取扇出数 | 该字段和「RAT映射字段」「ROB的寄存器标识符字段」是同一类多比特物理寄存器号字段，随机翻转（见「ROB的旧物理寄存器字段·单比特翻转」/「ROB的旧物理寄存器字段·双比特翻转」）大概率落到空闲表里的号，squash 时依赖检查失败直接崩溃；换成另一个活跃寄存器号后，squash 会错误释放/覆盖一个正在用的寄存器，预期 SDC 概率明显高于随机翻转版本 | 此前只给这个字段配了翻转模型，没配换值，是本方案审查后发现的疏漏——old-phys 字段的功能和 RAT映射字段、ROB的寄存器标识符字段完全同构（都是「物理寄存器号」字段），换值模型在那两处已经验证过有效，这里补齐是保持设计一致性，不是新增一个未经验证的思路 | 无 |
| D39 | R40 | Int Dispatch/ROB | ROB的旧物理寄存器字段·卡死 | ROB 项的 old-physical-register 字段（squash 回滚要用） | 卡死（结构化） | 运行开始时随机选一个比特位置，永久固定为 0 或 1，只在该字段被 squash 路径读取时才会表现出来 | F5 | —（不涉及事件触发） | GAP | 事件覆盖计数 | squash 恢复后架构寄存器状态与黄金参照的分歧，重点看同一种错误模式是否在多次 squash 中重复出现 | 和「ROB的旧物理寄存器字段·换值」不同：这行是「翻转」性质的卡死，不保证换到的号是活跃寄存器，预期效应更接近「ROB的旧物理寄存器字段·单比特翻转」而不是「ROB的旧物理寄存器字段·换值」；价值在于验证「持久缺陷+多次 squash 反复命中」是否比「单次瞬态命中一次 squash」更容易观察到累积效应 | old-phys 只在 squash 路径被读取，所以卡死的「持续存在」和「实际起作用」是两件事——defect 从运行一开始就在，但要等到 squash 发生才会被读到，因此计数基准仍按事件覆盖计数，不按运行计数 | 无 |
| D40 | R41 | Int Dispatch/ROB | ROB项整体·读到旧数据 | ROB 项（PC、寄存器标识符、old-phys 等字段构成的整条记录）——这里模拟的是整条记录没被真正清空覆盖，不是某一个字段单独出错 | 错位拼接（结构化）：读到旧数据 | 在该 ROB 槽位即将被新指令分派占用、原本该整条清空重写的那个周期，注入让写入静默失效，commit 阶段读到的是上一个占用者（早已提交或早已 squash 掉的旧指令）残留的完整记录 | —（不适用固定间隔） | 该 ROB 槽位被新指令分派写入覆盖的瞬间 | CoreMark、Embench | 事件覆盖计数 | 该槽位分派次数与实际清空重写次数的差值；commit 阶段读到「已经提交过的旧记录」的发生率 | 旧记录本身在它自己的生命周期里是完全合法、已经被正确处理过的数据，不会触发任何依赖检查失败——这是本方案里唯一能绕开「TC'23 式依赖检查」这道屏障、又不需要专门构造换值目标的机制，预期 SDC 概率很高 | TC'23 测的翻转、本方案前面补的换值/卡死，针对的都是「这一次写入的值本身被打坏了」；这行针对的是「上一次的值压根没被换掉」，是完全不同的故障根源，ROB 项是被高频复用的槽位（128 项循环使用），复用速度越快，「旧记录还没被真正冲掉」这类时序缺陷暴露的机会越多 | 可与 MICRO'24 DelayAVF 的机制类比：该论文描述字线延迟导致读操作实际锁存的是上一个地址的旧数据，这份旧数据本身是完好的、校验配套的合法数据，ECC 检查完全通过；但 DelayAVF 针对的是 Ibex 寄存器堆的时延故障模型，不是 gem5 里的功能级注入，是机制类比，不是直接对照 |
| D41 | R42 | Int Dispatch/ROB | ROB头指针·单比特翻转 | ROB 头指针（下一个待 commit 位置） | 单比特翻转 | 单比特翻转 | F0 | —（不涉及事件触发） | CoreMark | 运行计数 | commit 顺序与程序序的偏差；跳过提交或重复提交的次数 | 预期多数触发 gem5 内部一致性检查（如果存在）直接 Crash；少数指针错位后仍落在合法范围内的情形可能导致乱序提交，产生 SDC | 和尾指针（「ROB尾指针·单比特翻转」/「ROB尾指针·双比特翻转」）一起构成 ROB 管理逻辑的结构性弱点探测，头尾指针翻转的效应不对称（头指针影响提交，尾指针影响分配），分开设计便于归因 | 无 |
| D42 | R43 | Int Dispatch/ROB | ROB头指针·双比特翻转 | ROB 头指针 | 双比特翻转 | 双比特翻转，位置随机 | F0 | —（不涉及事件触发） | CoreMark | 运行计数 | 同 「ROB头指针·单比特翻转」 | 128 项对应 7 位指针字段，双比特翻转造成的位置偏移通常更大，预期越界概率高于 「ROB头指针·单比特翻转」，Crash 占比预期更高 | 与 「ROB头指针·单比特翻转」 配对，补齐头指针的单/双比特对照 | 无 |
| D43 | R44 | Int Dispatch/ROB | ROB头指针·卡死 | ROB 头指针（下一个待 commit 位置） | 卡死（结构化） | 运行开始时随机选指针的一个比特位置，永久固定为 0 或 1 | F5 | —（不涉及事件触发） | CoreMark | 运行计数 | commit 顺序与程序序的持续性偏差；同一种错位模式重复出现的次数 | 同「空闲表头/尾指针·卡死」的逻辑在 ROB 头指针上的复现，预期效应模式相近 | 与「ROB尾指针·卡死」一起补齐头/尾指针的卡死对照，保持和「ROB头指针·单比特翻转」/「ROB头指针·双比特翻转」同等的设计粒度 | 无 |
| D44 | R45 | Int Dispatch/ROB | ROB尾指针·单比特翻转 | ROB 尾指针（下一个待分配位置） | 单比特翻转 | 单比特翻转 | F0 | —（不涉及事件触发） | CoreMark | 运行计数 | 分配冲突次数（新指令被分配到仍在用的项） | 预期效应接近 「空闲表·重复分配(固定间隔)」（Free List 重复分配）：可能导致新分配的 ROB 项覆盖仍在用的项，产生数据损坏类 SDC | 与 「ROB头指针·单比特翻转」 对照，验证「同一类指针故障在不同端点（头/尾）的 SDC 敏感性是否对称」，是本方案里少有的可以直接做内部对照的一对 | 无 |
| D45 | R46 | Int Dispatch/ROB | ROB尾指针·双比特翻转 | ROB 尾指针 | 双比特翻转 | 双比特翻转，位置随机 | F0 | —（不涉及事件触发） | CoreMark | 运行计数 | 同 「ROB尾指针·单比特翻转」 | 同 「ROB头指针·双比特翻转」 的逻辑在尾指针上的复现 | 与 「ROB尾指针·单比特翻转」、「ROB头指针·双比特翻转」 一起构成头/尾指针 x 单/双比特的完整 2x2 对照组 | 无 |
| D46 | R47 | Int Dispatch/ROB | ROB尾指针·卡死 | ROB 尾指针（下一个待分配位置） | 卡死（结构化） | 运行开始时随机选指针的一个比特位置，永久固定为 0 或 1 | F5 | —（不涉及事件触发） | CoreMark | 运行计数 | 分配冲突的持续性重复发生率 | 同「ROB头指针·卡死」的逻辑在尾指针上的复现 | 与「ROB头指针·卡死」构成头/尾指针卡死对照的完整一对 | 无 |
| D47 | R48 | Int Dispatch/ROB | Int IQ的ready位·提前置位 | Int Issue Queue（64 项）项的 ready 位——该字段本身为单比特控制位，无多比特版本 | 状态（结构化）：提前置位 | 操作数还未算完就把 ready 位标记为就绪，执行单元读到脏数据/未初始化数据 | F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 执行单元读到的操作数与真实就绪值的偏差；FU 端检测到的异常操作数比例 | 预期 SDC 概率较高，逻辑与 「ROB的done位·提前置位(固定间隔)」（done 位提前置位）同源，都是「状态位撒谎导致下游读到未完成的数据」这一类结构化故障的代表 | IQ 的 ready 位同样不参与 gem5 的依赖图检查（那道检查只管寄存器号是否被正确分配，不管数值是否真的算完），是又一个绕开检查机制的入口 | 无 |
| D48 | R49 | Int Dispatch/ROB | Int IQ的ready位·永不置位(固定间隔) | Int Issue Queue 项的 ready 位——该字段本身为单比特控制位，无多比特版本 | 状态（结构化）：永不置位 | 该 µop 的操作数已就绪但 ready 位一直不被置位，µop 永久卡在 IQ 里无法发射 | F1 | —（不涉及事件触发） | ROB 填满核 | 运行计数 | IQ 占用率；发射停滞持续时间 | 预期主导结果是 Timeout，与 「ROB的done位·延迟置位(固定间隔)」、「空闲表·丢失释放」 同属「资源耗尽」类，用来在 IQ 这一层补齐前兆特征观测 | 和 「ROB的done位·延迟置位(固定间隔)」（ROB done 位延迟置位）对照，检验「资源耗尽型」故障的前兆信号在 ROB 和 IQ 两层是否有相同的可观测模式，产出是趋势曲线 | 无 |
| D49 | R50 | Int Dispatch/ROB | Int IQ的ready位·永不置位(事件触发) | Int Issue Queue 项的 ready 位——该字段本身为单比特控制位，无多比特版本 | 状态（结构化）：永不置位 | 该 µop 的操作数已就绪但 ready 位一直不被置位，µop 永久卡在 IQ 里无法发射 | —（不适用固定间隔） | IQ 占用超过 80%（约 51/64 项） | ROB 填满核 | 事件覆盖计数 | IQ 占用率；发射停滞持续时间 | 预期主导结果是 Timeout，与 「ROB的done位·延迟置位(固定间隔)」、「空闲表·丢失释放」 同属「资源耗尽」类，用来在 IQ 这一层补齐前兆特征观测；精确瞄准「IQ 快满」这个窗口，预期比固定间隔版更高效地命中该场景 | 和 「ROB的done位·延迟置位(固定间隔)」（ROB done 位延迟置位）对照，检验「资源耗尽型」故障的前兆信号在 ROB 和 IQ 两层是否有相同的可观测模式，产出是趋势曲线；固定间隔版已经能覆盖这个场景，但命中「IQ 恰好快满」这个窗口纯属随缘；这行精确瞄准该窗口 | 无 |
| D50 | R51 | Int Dispatch/ROB | Int IQ的tag字段·换值 | Int Issue Queue 项的源操作数 tag 字段（等待哪个物理寄存器就绪） | 换值（结构化） | 把等待的物理寄存器 tag 改成另一个号，导致读到错误的操作数来源 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 执行单元读到的操作数来源与预期来源的偏差 | 和 「RAT映射字段·换值(固定间隔)」/「ROB的寄存器标识符字段·换值」 同一思路在 tag 匹配环节的复现，预期能产生 SDC，因为改后的 tag 大概率仍对应一个合法的、有真实数据的物理寄存器 | tag 匹配失败通常不会像寄存器号越界那样触发硬性检查，是「换值」类模型里最接近真实电路行为（CAM 匹配错误）的一处 | 无 |
| D51 | R52 | Int Dispatch/ROB | Int IQ的tag字段·单比特翻转 | Int Issue Queue（64 项）项的源操作数 tag 字段（等待哪个物理寄存器就绪） | 单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 执行单元读到的操作数来源与预期来源的偏差；tag 匹配失败（未命中任何物理寄存器）的比例 | tag 字段本身没有类似寄存器号的稀疏合法性约束（tag 只是一个索引数字，翻转后大概率仍指向一个存在的物理寄存器位置），预期和「Int IQ的tag字段·换值」效果接近，不会像 opcode 那样出现翻转/换值的明显落差——这行的价值是给「换值」提供一个对照基线，验证这个字段是否真的需要专门的结构化模型，还是翻转已经够用 | 此前只给这个位置设计了「换值」，没配翻转基线，和 opcode/RAT/ROB 寄存器标识符字段的设计模式不一致——那几处都是先有翻转基线、再用换值验证增量，tag 字段这里补上，才能回答「换值到底比翻转多测出多少」这个问题 | 无 |
| D52 | R53 | Int Dispatch/ROB | Int IQ的tag字段·双比特翻转 | Int Issue Queue（64 项）项的源操作数 tag 字段 | 双比特翻转 | 双比特翻转，位置随机 | F0 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 同「Int IQ的tag字段·单比特翻转」 | 同「Int IQ的tag字段·单比特翻转」的逻辑，双比特版本补齐单/双比特对照 | 与「Int IQ的tag字段·单比特翻转」配对，补齐 tag 字段的单/双比特对照，保持和矩阵里其它多比特字段一致的设计粒度 | 无 |
| D53 | R54 | Int Dispatch/ROB | Int IQ的tag字段·卡死 | Int Issue Queue（64 项）项的源操作数 tag 字段 | 卡死（结构化） | 运行开始时随机选一个比特位置，永久固定为 0 或 1，该 IQ 项占用期间持续带着缺陷 | F5 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 同「Int IQ的tag字段·单比特翻转」，另外记录该 IQ 项占用期间是否反复触发同一种 tag 匹配偏差 | 预期效应接近「Int IQ的tag字段·单比特翻转」；价值在于 IQ 项占用时间通常比 ROB 项短，卡死效应在这类短生命周期结构上是否还能积累出可观测的持续性影响，需要实验验证 | 补齐 IQ 存储型字段的卡死覆盖 | 无 |
| D54 | R55 | Int Dispatch/ROB | Int IQ的tag字段·读到旧数据 | Int Issue Queue（64 项）项的源操作数 tag 字段 | 错位拼接（结构化）：读到旧数据 | 在该 IQ 槽位被新 µop 占用、tag 字段本该被重写的那个周期，注入让写入静默失效，执行单元发射时读到的是上一个占用者残留的旧 tag，转而等待/读取一个早已不相关的物理寄存器 | —（不适用固定间隔） | 该 IQ 槽位被新 µop 写入覆盖的瞬间 | CoreMark、Embench | 事件覆盖计数 | 该槽位占用次数与实际 tag 更新次数的差值；执行单元读到的操作数来源与预期来源的偏差 | 旧 tag 大概率仍指向一个存在的、有真实数据的物理寄存器（只是不是这条指令该等的那个），预期 SDC 概率高于「Int IQ的tag字段·单比特翻转」，和「Int IQ的tag字段·换值」的效应量级接近，但触发条件（写入时序缺陷 vs 主动构造替换值）完全不同 | IQ 槽位周转速度通常比 ROB 更快（发射后很快释放），「读到旧数据」这类写入时序缺陷在这里的暴露窗口更短但更频繁，是对「ROB项整体·读到旧数据」在更高周转率结构上的补充验证 | 可与 MICRO'24 DelayAVF 的机制类比：该论文描述字线延迟导致读操作实际锁存的是上一个地址的旧数据，这份旧数据本身是完好的、校验配套的合法数据，ECC 检查完全通过；但 DelayAVF 针对的是 Ibex 寄存器堆的时延故障模型，不是 gem5 里的功能级注入，是机制类比，不是直接对照 |
| D55 | R56 | Int Dispatch/ROB | 分发端口选择逻辑·状态翻转 | 分发端口选择逻辑（该 µop 该送去哪个执行单元/Issue Queue 的判定，如 ALU vs MDU）——按 3 ALU + 1 MDU 的实现，判定近似二元，无独立多比特版本 | 状态（结构化） | 把该走 ALU 的 µop 错误路由到 MDU，或反之 | F1 / F2 | —（不涉及事件触发） | CoreMark、Embench | 运行计数 | 路由错误后执行单元报告的操作类型不匹配次数 | 如果目标单元语义完全不兼容（如整数 vs 浮点），预期直接崩溃；如果凑巧都是简单整数运算单元，可能产生错误结果但不崩溃，需要实验验证而非预设，这一格的「预期结果」置信度较低，标注为探索性 | 对应总表里执行端口配置（3 ALU + 独立 MDU）的路由环节，是分发逻辑里少数几个「结构化故障是否致命」高度依赖具体硬件实现细节、不能靠纯逻辑推理定论的位置 | 无（探索性，预期结果置信度低） |

## FP/SIMD Decode（6 行：D56–D61）

| ID | Excel行 | 单元 | 位置标签 | 注入位置 | 故障类型 | 故障模型（具体做法） | 适用频率 | 事件触发条件 | 适用负载 | 计数基准 | L1 重点观测点 | 预期结果与依据 | 设计理由 | 可对照文献 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| D56 | R57 | FP/SIMD Decode | FP opcode字段·单比特翻转 | 译码输出锁存器的 opcode 字段（FP/SIMD 指令） | 单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 | 非法指令异常计数；FP/SIMD 发射队列路由错误计数 | 逻辑与 「opcode字段·单比特翻转」 相同：预期多数变成非法指令→Crash，SDC 概率很低 | 复用单元 1（Int Decode）「opcode字段·单比特翻转」 的设计逻辑，前端译码机制是共用的，此行验证同一机制在 FP/SIMD 指令流上的表现是否一致，作为跨单元交叉验证 | 无 |
| D57 | R58 | FP/SIMD Decode | FP opcode字段·双比特翻转 | 译码输出锁存器的 opcode 字段（FP/SIMD 指令） | 双比特翻转 | 双比特翻转，位置随机 | F0 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 | 同 「FP opcode字段·单比特翻转」 | 对应 「opcode字段·双比特翻转」，预期崩溃率高于 「FP opcode字段·单比特翻转」 | 补齐 「opcode字段·双比特翻转」 在 FP/SIMD 指令流上的对照 | 无 |
| D58 | R59 | FP/SIMD Decode | FP opcode字段·换值 | 译码输出锁存器的 opcode 字段 | 换值（结构化） | 把 opcode 换成另一个合法、操作数格式兼容的 FP/SIMD opcode（如 FADD↔FSUB 同类算术指令互换） | F0 / F1 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 | 指令替换发生率 | 预期与 「opcode字段·换值」 类似，SDC 概率高于随机翻转 | 对应 「opcode字段·换值」，验证 Veritas「控制逻辑不易 SDC」论断在浮点指令流上是否同样能被结构化模型突破 | 无 |
| D59 | R60 | FP/SIMD Decode | V寄存器号字段·单比特翻转 | 译码输出锁存器的源/目的 V 寄存器操作数字段（NEON 128 位寄存器编号，V0~V31） | 单比特翻转 | 单比特翻转 | F0 / F1 / F2 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 | 操作数强制切换发生率 | V 寄存器编号 5 位，翻转后落在合法范围（0~31）的概率高，预期比 opcode 翻转容易产生 SDC | 对应 「源/目的寄存器号字段·单比特翻转」，libjpeg-turbo 的手写 NEON 代码能提供比 PolyBench 更密集的向量寄存器访问，交叉验证不同代码风格下的表现 | 无 |
| D60 | R61 | FP/SIMD Decode | V寄存器号字段·双比特翻转 | 译码输出锁存器的源/目的 V 寄存器操作数字段 | 双比特翻转 | 双比特翻转，位置随机 | F0 / F1 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 | 同 「V寄存器号字段·单比特翻转」 | 对应 「源/目的寄存器号字段·双比特翻转」 | 补齐 「源/目的寄存器号字段·双比特翻转」 在向量寄存器字段上的对照 | 无 |
| D61 | R62 | FP/SIMD Decode | 指令路由判定位·状态翻转 | 指令识别/路由判定位（该指令送整数发射队列还是 FP/SIMD 发射队列）——该字段本身为单比特控制位，无多比特版本 | 状态（结构化） | 把 FP/SIMD 指令误判为整数指令（送错队列）/ 把整数指令误判为 FP 指令 | F1 / F2 | —（不涉及事件触发） | Embench（含整数/浮点混合的程序） | 运行计数 | 跨寄存器堆路由错误计数；FU 端类型不匹配异常 | 预期崩溃率高于单元 3 的对应位置（「分发端口选择逻辑·状态翻转」），因为整数和 FP/SIMD 走的是完全不同的寄存器堆和执行单元，类型不匹配大概率直接崩溃，不太可能有「凑巧兼容」的情形 | FP/SIMD Decode 相对 Int Decode 独有的位置，验证跨寄存器堆路由错误是否比单元 3 里的同类路由错误（「分发端口选择逻辑·状态翻转」，走同一类整数单元）崩溃率更高，用来检验「语义兼容程度决定 SDC 概率」这条推论 | 无 |

## FP/SIMD Rename（21 行：D62–D82）

| ID | Excel行 | 单元 | 位置标签 | 注入位置 | 故障类型 | 故障模型（具体做法） | 适用频率 | 事件触发条件 | 适用负载 | 计数基准 | L1 重点观测点 | 预期结果与依据 | 设计理由 | 可对照文献 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| D62 | R63 | FP/SIMD Rename | 标量FP RAT映射字段·单比特翻转 | 标量 FP RAT 表项（D/S 寄存器映射，numPhysFloatRegs=192 池） | 单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 | 同 「RAT映射字段·单比特翻转」，标量 FP 版 | 预期与 「RAT映射字段·单比特翻转」 一致，SDC 概率很低 | 对应 「RAT映射字段·单比特翻转」，验证同一机制在标量 FP 寄存器池上的表现；实验前须先核实 gem5 是否真的把标量 FP 和向量 NEON 当成两个独立寄存器类处理（见《OoO单元参数总表.md》单元 5 的说明），如果实际是同一套映射，此行与 「向量RAT映射字段·单比特翻转」 应合并，不重复跑 | 无（且机制待核实，见设计理由） |
| D63 | R64 | FP/SIMD Rename | 标量FP RAT映射字段·双比特翻转 | 标量 FP RAT 表项 | 双比特翻转 | 双比特翻转，位置随机 | F0 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 | 同 「标量FP RAT映射字段·单比特翻转」 | 对应 「RAT映射字段·双比特翻转」 | 补齐 「RAT映射字段·双比特翻转」 在标量 FP 池上的对照 | 无（同 「标量FP RAT映射字段·单比特翻转」 的机制核实前提） |
| D64 | R65 | FP/SIMD Rename | 标量FP RAT映射字段·换值 | 标量 FP RAT 表项 | 换值（结构化） | 改成另一个当前活跃的合法标量 FP 物理寄存器号 | F0 / F1 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 | 污染扇出数；浮点结果尾数低位污染占比（尾数低位错误比高位更难被察觉，属于 Veritas 论文里讨论过的现象） | 预期 SDC 概率高于 「标量FP RAT映射字段·单比特翻转」/「标量FP RAT映射字段·双比特翻转」，且浮点数据的「安静错误」特性（尾数低位翻转不改变数量级）预期让这里的 SDC 比整数版（「RAT映射字段·换值(固定间隔)」）更隐蔽、更难被 L2 层的粗粒度比对发现 | 对应 「RAT映射字段·换值(固定间隔)」，浮点数据天然比整数数据更容易产生「数值上接近但不相等」的隐蔽 SDC，这是浮点/向量场景相对整数场景的特有风险，值得专门设计验证 | 无 |
| D65 | R66 | FP/SIMD Rename | 标量FP RAT映射字段·卡死 | 标量 FP RAT 表项（D/S 寄存器映射，numPhysFloatRegs=192 池） | 卡死（结构化） | 运行开始时随机选一个比特位置，永久固定为 0 或 1 | F5 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 | 同「RAT映射字段·卡死」的浮点版 | 对应「RAT映射字段·卡死」，预期效应模式相近 | 补齐标量 FP RAT 的卡死覆盖，与「向量RAT映射字段·卡死」构成标量/向量池的卡死对照 | 无 |
| D66 | R67 | FP/SIMD Rename | 标量FP RAT映射字段·读到旧数据 | 标量 FP RAT 表项（D/S 寄存器映射，numPhysFloatRegs=192 池） | 错位拼接（结构化）：读到旧数据 | 同「RAT映射字段·读到旧数据」，标量 FP 池版 | —（不适用固定间隔） | 该表项被下一次重命名写入覆盖的瞬间 | PolyBench、Embench（浮点子集） | 事件覆盖计数 | 同「RAT映射字段·读到旧数据」 | 对应「RAT映射字段·读到旧数据」，预期效应模式相近，浮点数据的「安静错误」特性可能让旧值和新值在数量级上更接近、更难察觉 | 补齐标量 FP RAT 的「读到旧数据」覆盖，与「向量RAT映射字段·读到旧数据」构成标量/向量池对照 | 可与 MICRO'24 DelayAVF 的机制类比：该论文描述字线延迟导致读操作实际锁存的是上一个地址的旧数据，这份旧数据本身是完好的、校验配套的合法数据，ECC 检查完全通过；但 DelayAVF 针对的是 Ibex 寄存器堆的时延故障模型，不是 gem5 里的功能级注入，是机制类比，不是直接对照 |
| D67 | R68 | FP/SIMD Rename | 向量RAT映射字段·单比特翻转 | 向量/NEON RAT 表项（V/Q 寄存器映射，numPhysVecRegs=48 池） | 单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 | 同 「RAT映射字段·单比特翻转」，向量版 | 预期与 「RAT映射字段·单比特翻转」 一致 | 对应 「RAT映射字段·单比特翻转」；48 项的向量池远小于 192 项的标量池，同样注入频率下向量池的相对压力更大，此行同时服务于「机制验证」和「小容量池是否更敏感」两个目的 | 无（待核实见 「标量FP RAT映射字段·单比特翻转」） |
| D68 | R69 | FP/SIMD Rename | 向量RAT映射字段·双比特翻转 | 向量/NEON RAT 表项 | 双比特翻转 | 双比特翻转，位置随机 | F0 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 | 同 「向量RAT映射字段·单比特翻转」 | 向量池索引只需 6 位编码（48 项），比标量池的 8 位（192 项）更窄，双比特翻转造成的相对偏移更大，预期比 「标量FP RAT映射字段·双比特翻转」 更容易越界，Crash 占比预期更高 | 与 「标量FP RAT映射字段·双比特翻转」 构成标量池/向量池的双比特翻转横向对照，验证「池越小、编码位宽越窄，多比特翻转越容易破坏合法性」这条推论 | 无 |
| D69 | R70 | FP/SIMD Rename | 向量RAT映射字段·换值 | 向量/NEON RAT 表项 | 换值（结构化） | 改成另一个当前活跃的合法向量物理寄存器号 | F0 / F1 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 | 污染扇出数；污染是否只影响部分 lane（SIMD 特有的部分污染现象） | 预期 SDC 概率高于 「向量RAT映射字段·单比特翻转」/「向量RAT映射字段·双比特翻转」，逻辑同 「标量FP RAT映射字段·换值」；额外预期：由于 NEON 是 128 位整块寄存器，「换值」后污染大概率是整个寄存器一起错，而不是部分 lane，这点需要和真正的数据类故障（如 L1d-Cache 里字节级污染）做对比才能确认，这里先记录预期 | 对应 「RAT映射字段·换值(固定间隔)」 在向量寄存器上的复现，是本方案「向量寄存器堆结构化故障」的核心格 | 无 |
| D70 | R71 | FP/SIMD Rename | 向量RAT映射字段·卡死 | 向量/NEON RAT 表项（V/Q 寄存器映射，numPhysVecRegs=48 池） | 卡死（结构化） | 运行开始时随机选一个比特位置，永久固定为 0 或 1 | F5 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 | 同「RAT映射字段·卡死」的向量版，另外对比向量池（48 项）与标量池（192 项）卡死后曝光次数的差异 | 48 项的向量池比 192 项的标量池小，同一运行时长内该池的每一项被重命名的频率更高，预期「标量FP RAT映射字段·卡死」/「向量RAT映射字段·卡死」两行的曝光次数会有系统性差异，即使 SDC 概率本身接近 | 与「标量FP RAT映射字段·卡死」构成标量/向量池卡死对照，呼应单元 5 里「小容量池是否更敏感」这条既有对比线 | 无 |
| D71 | R72 | FP/SIMD Rename | 向量RAT映射字段·读到旧数据 | 向量/NEON RAT 表项（V/Q 寄存器映射，numPhysVecRegs=48 池） | 错位拼接（结构化）：读到旧数据 | 同「RAT映射字段·读到旧数据」，向量池版 | —（不适用固定间隔） | 该表项被下一次重命名写入覆盖的瞬间 | PolyBench、libjpeg-turbo | 事件覆盖计数 | 同「RAT映射字段·读到旧数据」，另外对比向量池（48 项）比标量池（192 项）更新更频繁，是否让「旧数据」的时间跨度更短、更难与正确值区分 | 48 项的池子重用率更高，同一物理寄存器号在短时间内被重新分配的概率更大，旧映射和新映射的语义差异可能更细微 | 补齐向量 RAT 的「读到旧数据」覆盖，呼应单元 5 里「小容量池是否更敏感」这条既有对比线 | 可与 MICRO'24 DelayAVF 的机制类比：该论文描述字线延迟导致读操作实际锁存的是上一个地址的旧数据，这份旧数据本身是完好的、校验配套的合法数据，ECC 检查完全通过；但 DelayAVF 针对的是 Ibex 寄存器堆的时延故障模型，不是 gem5 里的功能级注入，是机制类比，不是直接对照 |
| D72 | R73 | FP/SIMD Rename | 标量FP空闲表·重复分配(固定间隔) | 标量 FP Free List 队列项（192 项池） | 状态（结构化）：重复分配 | 同 「空闲表·重复分配(固定间隔)」 逻辑，标量池版 | F1 / F2 | —（不涉及事件触发） | 长依赖链压力核 | 运行计数 | 重复分配计数（标量池） | 预期与 「空闲表·重复分配(固定间隔)」 同源，SDC 与 Crash 并存 | 对应 「空闲表·重复分配(固定间隔)」 在标量 FP 池上的复现，与 「向量空闲表·重复分配(固定间隔)」（向量池版）一起构成「池容量大小是否影响结构化重复分配的敏感性」的系统对比 | 无 |
| D73 | R74 | FP/SIMD Rename | 标量FP空闲表·重复分配(事件触发) | 标量 FP Free List 队列项（192 项池） | 状态（结构化）：重复分配 | 同 「空闲表·重复分配(固定间隔)」 逻辑，标量池版 | —（不适用固定间隔） | 标量池剩余项数低于阈值（建议 ≤12 项） | 长依赖链压力核 | 事件覆盖计数 | 重复分配计数（标量池） | 预期与 「空闲表·重复分配(固定间隔)」 同源，SDC 与 Crash 并存；精确瞄准「标量池快见底」这个窗口，预期比固定间隔版更高效地命中该场景 | 对应 「空闲表·重复分配(固定间隔)」 在标量 FP 池上的复现，与 「向量空闲表·重复分配(固定间隔)」（向量池版）一起构成「池容量大小是否影响结构化重复分配的敏感性」的系统对比；固定间隔版已经能覆盖这个场景，但命中「标量池恰好快满」这个窗口纯属随缘；这行精确瞄准该窗口 | 无 |
| D74 | R75 | FP/SIMD Rename | 向量空闲表·重复分配(固定间隔) | 向量/NEON Free List 队列项（48 项池） | 状态（结构化）：重复分配 | 同 「空闲表·重复分配(固定间隔)」 逻辑，向量池版 | F1 / F2 | —（不涉及事件触发） | 长依赖链压力核（向量版：用向量累加链） | 运行计数 | 重复分配计数（向量池） | 预期向量池（48 项）比标量池（192 项）更容易被压到低水位、更容易复现结构化重复分配效应，是验证「小容量结构更脆弱」这一直觉的直接实验 | 对应 「空闲表·重复分配(固定间隔)」，向量池远小于标量池是 gem5 这份配置本身的特点（非 920 真实规格，见《OoO单元参数总表.md》），此行专门检验这个容量差异对 SDC 敏感性的影响，为后续判断「要不要单独扩大向量池」提供实验依据 | 无 |
| D75 | R76 | FP/SIMD Rename | 向量空闲表·重复分配(事件触发) | 向量/NEON Free List 队列项（48 项池） | 状态（结构化）：重复分配 | 同 「空闲表·重复分配(固定间隔)」 逻辑，向量池版 | —（不适用固定间隔） | 向量池剩余项数低于阈值（建议 ≤6 项，按 48 项池容量等比例设置） | 长依赖链压力核（向量版：用向量累加链） | 事件覆盖计数 | 重复分配计数（向量池） | 预期向量池（48 项）比标量池（192 项）更容易被压到低水位、更容易复现结构化重复分配效应，是验证「小容量结构更脆弱」这一直觉的直接实验；精确瞄准「向量池快见底」这个窗口，预期比固定间隔版更高效地命中该场景 | 对应 「空闲表·重复分配(固定间隔)」，向量池远小于标量池是 gem5 这份配置本身的特点（非 920 真实规格，见《OoO单元参数总表.md》），此行专门检验这个容量差异对 SDC 敏感性的影响，为后续判断「要不要单独扩大向量池」提供实验依据；固定间隔版已经能覆盖这个场景，但命中「向量池恰好快满」这个窗口纯属随缘；这行精确瞄准该窗口 | 无 |
| D76 | R77 | FP/SIMD Rename | 标量FP空闲表·丢失释放 | 标量 FP Free List 队列项 | 时序（结构化）：丢失释放 | 同 「空闲表·丢失释放」 逻辑，标量池版 | F1（持续到运行结束） | —（不涉及事件触发） | PolyBench | 运行计数 | 标量池尺寸随时间下降趋势 | 预期与 「空闲表·丢失释放」 同源，主导结果是 Timeout | 对应 「空闲表·丢失释放」，与 「向量空闲表·丢失释放」 一起构成「池容量大小如何影响 SDC/Timeout 前兆出现时机」的系统对比中的标量池一侧，产出是趋势曲线 | 无 |
| D77 | R78 | FP/SIMD Rename | 向量空闲表·丢失释放 | 向量/NEON Free List 队列项 | 时序（结构化）：丢失释放 | 同 「空闲表·丢失释放」 逻辑，向量池版 | F1（持续到运行结束） | —（不涉及事件触发） | 长依赖链压力核（向量版） | 运行计数 | 向量池尺寸随时间下降趋势，与 「标量FP空闲表·丢失释放」 的标量池曲线对比 | 预期向量池更快耗尽、更快出现 Timeout，同一时间窗口内向量池的「前兆信号」应该比标量池出现得更早 | 与 「标量FP空闲表·丢失释放」 构成标量/向量池的前兆时机对比，产出是趋势曲线 | 无 |
| D78 | R79 | FP/SIMD Rename | SVE谓词RAT映射字段·单比特翻转 | SVE 谓词寄存器 RAT（numPhysVecPredRegs=32 池，仅 SVE+SME 版配置适用） | 单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench（SVE 向量化版） | 运行计数 | SVE 通道使能（predicate lane enable）错误率；被错误屏蔽/错误启用的 lane 数 | 预期谓词翻转会导致部分 lane 的计算结果被错误地纳入或排除，是 SIMD/SVE 场景独有的「部分污染」故障，预期 SDC 概率较高且传播方式与整数/标量故障有本质不同 | **仅在使用 SVE+SME 版 gem5 配置时适用，920 本身无 SVE，这一行不用于 920 建模，只在做 N2/N3/V2 等有 SVE 的芯片对比实验时才启用**，见《OoO单元参数总表.md》的 SVE+SME 版说明；PolyBench 本身不含 SVE 指令，需要改写为 SVE 向量化版本或用支持 SVE 的编译器自动向量化产出 | 无（SVE 专属，探索性） |
| D79 | R80 | FP/SIMD Rename | SVE谓词RAT映射字段·双比特翻转 | SVE 谓词寄存器 RAT | 双比特翻转 | 双比特翻转，位置随机 | F0 | —（不涉及事件触发） | PolyBench（SVE 向量化版） | 运行计数 | 同 「SVE谓词RAT映射字段·单比特翻转」 | 谓词寄存器池只需 5 位编码（32 项），双比特翻转造成的相对偏移比 「向量RAT映射字段·双比特翻转」（向量池 6 位）更大，预期 Crash 占比高于 「SVE谓词RAT映射字段·单比特翻转」 | 与 「SVE谓词RAT映射字段·单比特翻转」 配对，补齐 SVE 谓词寄存器的单/双比特对照；同样仅在 SVE+SME 版配置下适用 | 无（SVE 专属，探索性） |
| D80 | R81 | FP/SIMD Rename | SVE谓词RAT映射字段·换值 | SVE 谓词寄存器 RAT（numPhysVecPredRegs=32 池，仅 SVE+SME 版配置适用） | 换值（结构化） | 改成另一个当前活跃的合法谓词物理寄存器号 | F0 / F1 | —（不涉及事件触发） | PolyBench（SVE 向量化版） | 运行计数 | SVE 通道使能错误率；被错误屏蔽/错误启用的 lane 数（同「SVE谓词RAT映射字段·单比特翻转」，但换值后预期命中率更高） | 和标量/向量 RAT 的换值模型（「标量FP RAT映射字段·换值」「向量RAT映射字段·换值」）同一逻辑，预期 SDC 概率高于「SVE谓词RAT映射字段·单比特翻转」/「SVE谓词RAT映射字段·双比特翻转」 | SVE 谓词 RAT 和标量 FP RAT、向量 RAT 是同一类多比特物理寄存器号映射字段，此前只给它配了翻转，没配换值，是设计疏漏，这里补齐保持三类 RAT 的设计对称；仅在 SVE+SME 版配置下适用，920 本身无 SVE | 无（SVE 专属，探索性） |
| D81 | R82 | FP/SIMD Rename | SVE谓词RAT映射字段·卡死 | SVE 谓词寄存器 RAT（numPhysVecPredRegs=32 池，仅 SVE+SME 版配置适用） | 卡死（结构化） | 运行开始时随机选一个比特位置，永久固定为 0 或 1 | F5 | —（不涉及事件触发） | PolyBench（SVE 向量化版） | 运行计数 | 同「RAT映射字段·卡死」的 SVE 谓词版 | 对应「RAT映射字段·卡死」，预期效应模式相近 | 补齐 SVE 谓词 RAT 的卡死覆盖，完成 Int/标量FP/向量/SVE谓词四类 RAT 的卡死对照；仅在 SVE+SME 版配置下适用，920 本身无 SVE | 无（SVE 专属，探索性） |
| D82 | R83 | FP/SIMD Rename | SVE谓词RAT映射字段·读到旧数据 | SVE 谓词寄存器 RAT（numPhysVecPredRegs=32 池，仅 SVE+SME 版配置适用） | 错位拼接（结构化）：读到旧数据 | 同「RAT映射字段·读到旧数据」，SVE 谓词池版 | —（不适用固定间隔） | 该表项被下一次重命名写入覆盖的瞬间 | PolyBench（SVE 向量化版） | 事件覆盖计数 | 同「RAT映射字段·读到旧数据」 | 对应「RAT映射字段·读到旧数据」，完成 Int/标量FP/向量/SVE谓词四类 RAT 的「读到旧数据」全覆盖 | 仅在 SVE+SME 版配置下适用，920 本身无 SVE | 可与 MICRO'24 DelayAVF 的机制类比：该论文描述字线延迟导致读操作实际锁存的是上一个地址的旧数据，这份旧数据本身是完好的、校验配套的合法数据，ECC 检查完全通过；但 DelayAVF 针对的是 Ibex 寄存器堆的时延故障模型，不是 gem5 里的功能级注入，是机制类比，不是直接对照（SVE 专属，探索性） |

## FP/SIMD Dispatch/ROB（9 行：D83–D91）

| ID | Excel行 | 单元 | 位置标签 | 注入位置 | 故障类型 | 故障模型（具体做法） | 适用频率 | 事件触发条件 | 适用负载 | 计数基准 | L1 重点观测点 | 预期结果与依据 | 设计理由 | 可对照文献 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| D83 | R84 | FP/SIMD Dispatch/ROB | ROB的PC字段(FP负载自检)·单比特翻转 | ROB 项的 PC 字段（与单元 3 完全共用同一个 128 项 ROB，不是独立结构）——内置自检，用 FP/SIMD 指令重跑 「ROB的PC字段·单比特翻转」 | 单比特翻转 | 同 「ROB的PC字段·单比特翻转」 模型，改用 FP/SIMD 指令触发 | F0 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 | 同单元 3 的 「ROB的PC字段·单比特翻转」 | 预期结果应与 「ROB的PC字段·单比特翻转」 用整数负载测出的 SDC≈0% 高度一致（同一个 ROB 结构，故障模型和硬件行为不因指令类型改变） | TC'23、IISWC'15、Cortex-A72 三方资料一致确认 ROB 是整数/FP 共用的统一结构，这里不重复设计全部 ROB 相关行，只挑 「ROB的PC字段·单比特翻转」（预期 SDC≈0% 的基线格）用 FP 负载重跑一遍作为内置自检——如果 FP 负载测出的结果和整数负载差异很大，说明注入器实现或「ROB 是统一结构」这个假设有问题，需要回头排查；样本量同样沿用统一的 2000 次，保证可比性 | TC'23 原论文正文：ROB 为统一结构 |
| D84 | R85 | FP/SIMD Dispatch/ROB | ROB的done位(FP负载自检，固定间隔)·提前置位 | ROB 项的 done/completed 状态位——内置自检，用 FP/SIMD 指令重跑 「ROB的done位·提前置位(固定间隔)」 | 状态（结构化）：提前置位 | 同 「ROB的done位·提前置位(固定间隔)」 模型，改用 FP/SIMD 指令触发 | F1 / F2 | —（不涉及事件触发） | PolyBench、ROB 填满核（浮点版） | 运行计数 | 同单元 3 的 「ROB的done位·提前置位(固定间隔)」 | 预期结果应与 「ROB的done位·提前置位(固定间隔)」 用整数负载测出的高 SDC 率高度一致 | 同 「ROB的PC字段(FP负载自检)·单比特翻转」 的自检逻辑，挑 「ROB的done位·提前置位(固定间隔)」（预期 SDC 最高的格）重跑一遍作为第二个自检点，覆盖「统一结构」假设在低 SDC 和高 SDC 两种预期下是否都成立；样本量同样沿用统一的 2000 次 | TC'23 原论文正文：ROB 为统一结构 |
| D85 | R86 | FP/SIMD Dispatch/ROB | ROB的done位(FP负载自检，事件触发)·提前置位 | ROB 项的 done/completed 状态位——内置自检，用 FP/SIMD 指令重跑 「ROB的done位·提前置位(固定间隔)」 | 状态（结构化）：提前置位 | 同 「ROB的done位·提前置位(固定间隔)」 模型，改用 FP/SIMD 指令触发 | —（不适用固定间隔） | ROB 占用超过 80% | ROB 填满核（浮点版） | 事件覆盖计数 | 同单元 3 的 「ROB的done位·提前置位(固定间隔)」 | 预期结果应与 「ROB的done位·提前置位(固定间隔)」 用整数负载测出的高 SDC 率高度一致；精确瞄准「ROB 快满」这个窗口，预期比固定间隔版更高效地命中该场景 | 同 「ROB的PC字段(FP负载自检)·单比特翻转」 的自检逻辑，挑 「ROB的done位·提前置位(固定间隔)」（预期 SDC 最高的格）重跑一遍作为第二个自检点，覆盖「统一结构」假设在低 SDC 和高 SDC 两种预期下是否都成立；样本量同样沿用统一的 2000 次；固定间隔版已经能覆盖这个场景，但命中「ROB 恰好快满」这个窗口纯属随缘；这行精确瞄准该窗口 | TC'23 原论文正文：ROB 为统一结构 |
| D86 | R87 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的ready位·提前置位 | FP/SIMD Issue Queue 项的 ready 位——该字段本身为单比特控制位，无多比特版本 | 状态（结构化）：提前置位 | 同 「Int IQ的ready位·提前置位」 逻辑，FP/SIMD 队列版 | F1 / F2 | —（不涉及事件触发） | PolyBench、Embench（浮点子集） | 运行计数 | 执行单元读到的浮点操作数与真实就绪值的偏差；尾数低位污染占比 | 预期与 「Int IQ的ready位·提前置位」 一致，SDC 概率较高；额外预期浮点场景的「尾数低位污染难以察觉」效应会让这里的隐蔽 SDC 比例高于整数版 | 对应 「Int IQ的ready位·提前置位」，与 「标量FP RAT映射字段·换值」 呼应，浮点数据路径上的「状态位撒谎」故障预期比整数路径更难被发现，是本方案里几个重点关注「隐蔽性」的位置之一 | 无 |
| D87 | R88 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的tag字段·换值 | FP/SIMD Issue Queue 项的源操作数 tag 字段 | 换值（结构化） | 同 「Int IQ的tag字段·换值」 逻辑，FP/SIMD 队列版 | F0 / F1 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 | 同 「Int IQ的tag字段·换值」 的浮点/向量版 | 预期与 「Int IQ的tag字段·换值」 一致 | 对应 「Int IQ的tag字段·换值」，补齐 FP/SIMD 发射队列侧的 tag 匹配故障覆盖，与单元 3 构成整数/浮点两条路径的完整对照 | 无 |
| D88 | R89 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的tag字段·单比特翻转 | FP/SIMD Issue Queue 项的源操作数 tag 字段 | 单比特翻转 | 单比特翻转 | F0 / F1 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 | 同「Int IQ的tag字段·单比特翻转」的浮点/向量版 | 对应「Int IQ的tag字段·单比特翻转」，预期和「FP/SIMD IQ的tag字段·换值」效果接近 | 对应 Int Dispatch/ROB 的 tag 字段翻转基线补齐，与「FP/SIMD IQ的tag字段·换值」构成完整对照，同时和单元 3 的整数路径形成整数/浮点两条路径的完整对照 | 无 |
| D89 | R90 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的tag字段·双比特翻转 | FP/SIMD Issue Queue 项的源操作数 tag 字段 | 双比特翻转 | 双比特翻转，位置随机 | F0 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 | 同「FP/SIMD IQ的tag字段·单比特翻转」 | 同「Int IQ的tag字段·双比特翻转」的浮点/向量版 | 与「FP/SIMD IQ的tag字段·单比特翻转」配对，补齐单/双比特对照 | 无 |
| D90 | R91 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的tag字段·卡死 | FP/SIMD Issue Queue 项的源操作数 tag 字段 | 卡死（结构化） | 运行开始时随机选一个比特位置，永久固定为 0 或 1 | F5 | —（不涉及事件触发） | PolyBench、libjpeg-turbo | 运行计数 | 同「Int IQ的tag字段·卡死」的浮点/向量版 | 对应「Int IQ的tag字段·卡死」 | 补齐 FP/SIMD IQ tag 字段的卡死覆盖，完成整数/浮点两条路径的卡死对照 | 无 |
| D91 | R92 | FP/SIMD Dispatch/ROB | FP/SIMD IQ的tag字段·读到旧数据 | FP/SIMD Issue Queue 项的源操作数 tag 字段 | 错位拼接（结构化）：读到旧数据 | 同「Int IQ的tag字段·读到旧数据」，FP/SIMD 队列版 | —（不适用固定间隔） | 该 IQ 槽位被新 µop 写入覆盖的瞬间 | PolyBench、libjpeg-turbo | 事件覆盖计数 | 同「Int IQ的tag字段·读到旧数据」 | 对应「Int IQ的tag字段·读到旧数据」，完成整数/浮点两条路径在「读到旧数据」这类机制上的完整对照 | 补齐 FP/SIMD IQ tag 字段的「读到旧数据」覆盖 | 可与 MICRO'24 DelayAVF 的机制类比：该论文描述字线延迟导致读操作实际锁存的是上一个地址的旧数据，这份旧数据本身是完好的、校验配套的合法数据，ECC 检查完全通过；但 DelayAVF 针对的是 Ibex 寄存器堆的时延故障模型，不是 gem5 里的功能级注入，是机制类比，不是直接对照 |

