把 gem5-fs 目录下的内容提交到 git@github.com:wangxumarshall/gem5-fs.git 代码仓，然后在当前的gem5-fi中创建一个指向gem5-fs的submodule

---

把 gem5-fs 目录下的内容提交到 git@github.com:wangxumarshall/gem5-fs.git 代码仓，然后在当前的gem5-fi中创建一个指向gem5-fs的submodule

---

把 gem5-fs 目录下的内容提交到 git@github.com:wangxumarshall/gem5-fs.git 代码仓，然后在当前的gem5-fi中创建一个指向gem5-fs的submodule

---

把 gem5-fs 目录下的内容提交到 git@github.com:wangxumarshall/gem5-fs.git 代码仓，然后在当前的gem5-fi中创建一个指向gem5-fs的submodule