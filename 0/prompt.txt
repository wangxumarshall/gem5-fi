A session-scoped Stop hook is now active with condition: "完成后续所有工作：
  1. H6 定量谱可分（D1-only vs D2-only vs D1+D2）：D2 注入规范地址后执行流改变/卡住，需多 seed + 能恢复/对照状态。
  2. H7 ECC on/off spurious 率对照：低 prob 在 200M tick 全 0 注入（walk 仅 14 次，期望 0.014 命中），需 FS 到用户态多进程（高
  walk 密度）。
  3. FS 完整启动到 Linux bash：单核 130k inst/s，到 bash 需 1–3 亿指令 → 15min–2h 墙钟，未完成。


  P0 — FS 完整启动验证（解除 H6/H7 的根本前置条件）
  - 后台长跑 o3_chaos_fs.py（--listener-mode on + taskset 隔离 cpu179），目标跑到 Linux 用户态 bash。用 m5term/raw socket 3456
  抓真实启动日志，确认 rootfs 挂载 + 用户态可用。 
  - 预计 1–2h 墙钟；先跑一次无注入的基线确认能到 bash。

  P1 — H7 ECC on/off 定量对照（依赖 P0）
  - FS 到用户态后，walk 密度上升；用 --ptw-prob ~1e-4 + --ptw-ecc on/off + --ptw-bits 1/2 多 arm，量化 spurious 率随 ECC 变化，与
  core179 的 73 例 ESR 分布形状比对。
  - 关键可证伪点：ECC-on spurious≈0、ECC-off spurious>0。

  P2 — H6 D2-only 谱可分（依赖 P0，本轮更可行）
  - D2 load 密度充足（61081/400M），用多 seed + 低 prob 在 FS 用户态跑 D1-only/D2-only/D1+D2 三臂，判 D1-only 与 D2-only 的
  Crash/SDC 谱是否可区分（单/多缺陷的仿真侧代理）。
  - 需先精化 D2 注入使其可恢复（不卡死），或加 max-faults 限制注入数。

  P3 — D3 注入粒度精化
  - 当前 prob=0.5 破坏 PTE 致 fetch 卡住（Address ... outside physical memory）。改为低 prob + --max-faults
  造"瞬态可重试"而非永久破坏，让 spurious 能自然重试成功（复现 silicon 的 retry-OK 行为）。
 .". Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do. The hook will block stopping until the condition holds. It auto-clears once the condition is met — do not tell the user to run `/goal clear` after success; that's only for clearing a goal early.