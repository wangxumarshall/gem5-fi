A session-scoped Stop hook is now active with condition: "完成所有工作：  
  本轮已完成（实证）
  
  P3b — CHAOSPTW clearValidBit 模式（已 push a106c2b/fb8597b）
  - 解决 XOR 限制：data[off]^=mask 无法可靠制造 invalid PTE（0b01^0b11=0b10 仍 valid），629 注入全 benign。
  - 新增 clearValidBit（AND ~0x3 清零 bits[1:0]）：实证 numSpuriousFaults=629（100% spurious）。
  
  H7 内核态对照（已 push 7dd9225）
  - Linux 内核态 walk 密度突破：numHooksCalled=54074（57B tick），是早期 boot 的 10 倍——H7 不必到 bash。
  - 两机制实证：单bit ECC-on 0 注入 vs off 40 注入（ECC 纠正）；clearValidBit 40/40 spurious（制造）。
  
  P3c 进展（本轮进行中，代码已改但验证未完成）
  
  已完成的代码改动（未提交）：
  - 加 conditionalValidBit 参数：只对 0b01(block) PTE 翻 bit0（0b01→0b00 invalid），单 bit 翻转。设计目标：ECC-on 纠正→不 spurious，ECC-off 不纠正→spurious，同一实验内对照 ECC
  on/off（H7 完整可证伪点）。
  - 改了 CHAOSPTW.hh（成员）+ .py（参数）+ .cc（逻辑）+ o3_chaos_fs.py（透传）。
  - 发现并修复一个 bug：初版把 conditionalValidBit 加进了"绕过 ECC"条件，导致 ECC on/off 结果相同（都 spurious）。已改为 if (!clear_valid_bit && ptw_ecc && num_bits <
  2)——conditionalValidBit 走 ECC 纠正。

  待完成：
  1. relink gem5.opt（ECC 逻辑修复后）—— 本轮 relink 在后台跑（pid 1248231），但被用户中断，未确认完成。
  2. P3c 功能验证：conditionalValidBit + ECC on/off 短测，确认 ECC-on spurious≈0、ECC-off spurious>0。
  3. 提交 P3c patch。

  诚实未完成（全部后续工作）

  ┌────────┬─────────────────────────────┬───────────────────────────────────────┬───────────────────────────┐
  │ 优先级 │            工作             │                 状态                  │           阻塞            │
  ├────────┼─────────────────────────────┼───────────────────────────────────────┼───────────────────────────┤
  │ P3c    │ 条件注入 + H7 完整 ECC 对照 │ 代码已改，relink 被中断               │ 需完成 relink + 验证      │
  ├────────┼─────────────────────────────┼───────────────────────────────────────┼───────────────────────────┤
  │ P0     │ FS 到 bash                  │ 部分完成（到 7828 万指令/virtio_blk） │ 间歇启动失败 + 时间长     │
  ├────────┼─────────────────────────────┼───────────────────────────────────────┼───────────────────────────┤
  │ P2     │ H6 D2 谱可分                │ 未开始                                │ 依赖 P0-bash 或长内核态跑 │
  ├────────┼─────────────────────────────┼───────────────────────────────────────┼───────────────────────────┤
  │ P4     │ 多 seed 平均                │ 未开始                                │ H7 两臂路径不同           │". Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do. The hook will block stopping until the condition holds. It auto-clears once the condition is met — do not tell the user to run `/goal clear` after success; that's only for clearing a goal early.