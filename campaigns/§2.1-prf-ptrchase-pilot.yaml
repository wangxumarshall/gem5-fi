schema_version: arm-chaos-fi/v1
campaign_id: prf_ptrchase_pilot
injector: physreg
config: C2
mode: SE
grid:
  # §2.1 D method2 direction (Phase 5.6 follow-up): pointer-class register
  # on the pointer-chase workload. X10 = the list-walking pointer (method2's
  # find_busiest_group register). Prediction (§2.1 E): high bits -> P_DUE
  # (garbage pointer -> translation fault); low bits -> aliased pointer ->
  # SDC or Hang.
  phys_mode: [arch_frontend]
  target_index: [10]
  bit: [0, 31, 63]
  fault_model: [transient_bit_flip]
  protection_model: [none]
n_per_cell: 100
base_seed: 20260825
replay_pct: 5.0
workload:
  binary: workloads/directed/ptr_chase_long
  golden_id: ptrchaselong-golden-v1
  trigger_mode: cycle
  trigger_value: 100000
limits:
  max_faults: 1
hang_timeout: 300
