schema_version: arm-chaos-fi/v1
campaign_id: specleak_formal_x19
injector: rat
config: C2
mode: SE
grid:
  # §2.3 spec_leak follow-up (Phase 4.1b): the branchy X3/X9 result showed
  # short-lived loop vars get overwritten — the method1 leak-to-SDC pathway
  # needs a LONG-LIVED register whose live range crosses the squash
  # boundary. X19 = callee-saved accumulator class (method1's own target).
  target_index: [19]
  bit: []
  fault_model: [intermittent_burst]
  protection_model: [none]
n_per_cell: 384
base_seed: 20260825
replay_pct: 5.0
workload:
  binary: workloads/directed/branchy_reduce
  golden_id: branchyreduce-golden-v1
  trigger_mode: cycle
  trigger_value: 20000
limits:
  max_faults: 1
hang_timeout: 300
