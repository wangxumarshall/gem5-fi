schema_version: arm-chaos-fi/v1
campaign_id: fpu_formal_gemm
injector: fsu
config: C2
mode: SE
grid:
  phys_mode: [arch_frontend]
  target_index: [0]
  bit: []
  fault_model: [transient_bit_flip]
  protection_model: [none]
n_per_cell: 384
base_seed: 20260825
replay_pct: 5.0
workload:
  # FP-dense workload: neon_lane's FP-eligible event stream is too small
  # (Reach=17% with geometric skip) — gemm_float gives a dense FP stream.
  binary: workloads/directed/gemm_float_kernel
  golden_id: gemmfloat-golden-v1
  trigger_mode: cycle
  trigger_value: 20000
limits:
  max_faults: 1
hang_timeout: 300
