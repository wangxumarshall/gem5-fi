schema_version: arm-chaos-fi/v1
campaign_id: specleak_branchy_pilot
injector: rat
config: C2
mode: SE
grid:
  # §2.3 spec_leak pilot (Phase 4.1): method1 speculative-state leak.
  # fault.model intermittent_burst -> runner maps to --rename_mode spec_leak.
  # branchy_reduce = branch-heavy (rich wrong-path squash stream, the
  # precondition for a leak to matter); X3 = the method1 accumulator target.
  target_index: [3]
  bit: []
  fault_model: [intermittent_burst]
  protection_model: [none]
n_per_cell: 5
base_seed: 20260825
replay_pct: 0
workload:
  binary: workloads/directed/branchy_reduce
  golden_id: branchyreduce-golden-v1
  trigger_mode: cycle
  trigger_value: 20000
limits:
  max_faults: 1
hang_timeout: 300
