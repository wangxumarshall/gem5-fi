schema_version: arm-chaos-fi/v1
campaign_id: prf_h2_trigger_scan_80k
injector: physreg
config: C2
mode: SE
grid:
  phys_mode: [arch_frontend]
  # H2 follow-up trigger sweep at ROB=160: trigger=80000 (sibling of
  # prf_h2_trigger_scan at 20000; 50000 is the original H2 pilot row).
  target_index: [3]
  bit: [0]
  rob: [160]
  phys_int: [128, 160, 192]
  fault_model: [transient_bit_flip]
  protection_model: [none]
n_per_cell: 30
base_seed: 20260825
replay_pct: 5.0
workload:
  binary: workloads/directed/cholesky_numeric
  golden_id: cholesky-golden-v1
  trigger_mode: cycle
  trigger_value: 80000
limits:
  max_faults: 1
hang_timeout: 300
